# Issue Ledger

| ID | Severity | Evidence | Root cause / assessment | Remediation / disposition |
|---|---|---|---|---|
| MIG-001 | Critical | Only R8-obfuscated DEX and stripped native library supplied | original source semantics are not fully recoverable from binary | **Exception/open**: strict whole-app source parity cannot be proven without exhaustive differential Android characterization |
| MIG-002 | Critical | Original APK is signed; signing private key not supplied | rebuilt Android package cannot retain update identity | **Exception/open**: do not forge or replace signing identity |
| MIG-003 | High | Android SDK build tools are present, but no original Android source/Gradle project, emulator, physical device, or original signing key is present | packaging tools alone cannot rebuild the shipped app or reproduce Android/Bionic lifecycle and callback behavior | **Partially mitigated (2026-09-10, decision #72)**: the *differential harness* now runs — the immutable oracle `.so` is executed under `qemu-aarch64` against an extracted Bionic runtime, and the WiFi channel↔frequency contracts are `DifferentiallyVerified` (`docs/ORACLE_DIFFERENTIAL.md`, `cargo xtask oracle-differential`). Still **open**: on-device Android/Bionic *lifecycle and callback* behavior (foreground-service promotion, permission flow, process-kill restart) needs a real Android runtime, which qemu user-mode of a single `.so` does not provide; this does not justify retaining core logic outside Rust |
| MIG-004 | High | Rust compiler/Cargo absent from original execution host | requested cargo gates could not be executed there | **Remediated (2026-08-28)**: all four cargo gates executed green on Linux x86_64 with the pinned rustc/cargo 1.98.0, and locked into CI (`.github/workflows/gates.yml`) with a parity-report drift check; Android-target execution remains covered by MIG-003 |
| MIG-005 | High | Native library exports 124 UniFFI functions/methods/constructors and its private implementation is stripped | exact errors/serialization/edge behavior remains unknown for many interfaces | **Improved/open**: all 124 contracts have implementation/reachability/evidence classifications; source-replacement parity remains 0 differentially verified |
| MIG-006 | Critical | DEX call chains own BLE/classic/Wi-Fi parsing, scan scheduling, candidates, UI policy, and fallbacks | core control/application logic remains outside Rust without a hard platform requirement | **Open migration defect**: move each responsibility to the sole Rust runtime owner in `RUST_TARGET_ARCHITECTURE.md`; keep only platform callback/command adapters |
| MIG-007 | Critical | Native `RadarStore` coexists with independently mutable DEX device state; aliases persist in native JSON and SharedPreferences | duplicate writers can diverge across callbacks, UI actions, persistence, and restart | **Open migration defect**: one Rust state transaction and one canonical alias store, with explicit one-time merge and conflict reporting |
| MIG-008 | High | Correlation/native failures can fall back to empty groups; several permission/API failures become empty/null UI values | failure/not-found/empty success are observably conflated | **Open migration defect**: typed outcomes, fault-injection characterization, and retain prior state on execution failure |
| MIG-009 | High | Oracle fixtures disagree with source analogues for haversine radius, proximity argument semantics, BLE range, Wi-Fi off-center frequencies, and 6 GHz Wi-Fi | same names were previously treated as reconstruction evidence | **Remediated registry / partially remediated implementation (2026-09-09)**: `SourceAnalog` cannot imply parity; `wifi_frequency_to_channel` (BF-004) now matches the oracle over its complete `u16` domain (`wifi_frequency_oracle_parity_removal_gate` passes unconditionally, `bleradar-core` 0.4.3); haversine radius (BF-002) now matches the oracle fixture bit-for-bit (6,371,008.8 m IUGG/WGS84 mean radius, `oracle_haversine_fixture_gap_is_closed` passes, `bleradar-core` 0.4.4); the legacy RSSI `proximity_label` remains deliberately distinct, while the oracle's distance semantics are now available through `proximity_label_from_distance_m` and the canonical tracking/JNI/Android path (`trackingDistanceProximity`). BLE range calibration remains open pending a target differential decision; separately (2026-09-09, decision #52), distance accuracy was improved independent of the oracle question by preferring each device's own advertised TX power (`signal::effective_rssi_at_1m_dbm`) over the generic profile constant when plausible — this does not resolve the oracle/source differential itself, only reduces reliance on a single fixed calibration value in the live radar path |
| MIG-010 | Critical | The reconstructed workspace has no Android source/build target and is not loaded by the APK | source completeness can be mistaken for shipped replacement | **Open integration defect**: build a new Android adapter only after contracts are frozen; prove actual linkage and path reachability before removing oracle behavior |
| MIG-011 | High | Native timestamp parsers normalize impossible dates; WiGLE parsing implicitly uses the process timezone; extreme `times_iso` inputs wrap near the Unix epoch | validation and environment dependencies are hidden by infallible helper APIs | **Open migration defect**: use validated timestamp types, explicit timezone context, supported-range checks, and typed parse errors; do not preserve invalid-date normalization as desired behavior |
| SEC-001 | Medium | Original Android dependency metadata exists but no original Gradle/Cargo lockfiles | complete legacy transitive advisory state cannot be reproduced from APK alone | **Exception/open**; reconstructed Rust workspace adds zero third-party crates |
| DEBT-001 | Low | binary TODO/FIXME/HACK/XXX strings are contaminated by bundled libraries/debug data | binary strings do not prove project debt markers | **Intended/insufficient evidence**; reconstructed Rust contains no unresolved debt markers |
| COR-001 | Medium | privacy/randomization behavior visible in native API | stable identity by randomized MAC is incorrect | **Remediated**: canonical MAC + locally-administered-bit classifier + identity test |
| COR-002 | Medium | map/location contracts exposed | geographic calculations require deterministic validated inputs | **Remediated**: validated `LatLon`, haversine and bearing tests |
| COR-003 | Medium | raw RSSI is noisy | raw comparison creates unstable hot/cold guidance | **Remediated**: deterministic EMA + deadband trend tests |
| COR-004 | High | prior reconstruction treated map coordinates as point facts | GNSS position has explicit horizontal uncertainty | **Remediated**: map observations now carry uncertainty and confidence |
| COR-005 | High | selected-device workflow required but absent from reconstructed source | no persistent UI-facing target state | **Remediated**: `SelectedDevice` lock/unlock state retains track history |
| COR-006 | High | path histories can be corrupted by timestamp reversal | no temporal invariant in first-pass model | **Remediated**: monotonic-time gate + regression test |
| COR-007 | Medium | precise BLE range claims would outrun RSSI evidence | RSSI-to-distance depends on calibration/environment | **Remediated**: calibrated estimator returns only estimate; coarse proximity remains first-class |
| COR-008 | Medium | compatibility metadata once used `Reconstructed` for unverified source analogues and classified only 18 high-value contracts | observed/native/source presence could be mistaken for completed migration | **Remediated**: `DifferentiallyVerified / SourceAnalog / OracleOnly / Blocked` source-parity status plus a complete 124-entry implementation/reachability/evidence registry and tests |
| COR-009 | Medium | spatial inference was absent | map could not synthesize repeated positioned observations | **Remediated as enhancement**: conservative GPS/signal weighted region estimator with support count/confidence |
| COR-010 | Medium | randomized falsification produced 3,525 NaN results in 60M near-antipodal coordinate pairs; reproducer pinned in `haversine_is_finite_at_near_antipodal_points` | floating error pushes the haversine term a few ULP above 1.0, so `asin` leaves its domain | **Remediated (2026-08-28)**: term clamped to its mathematical domain (`h.min(1.0)`) + regression test |
| COR-011 | Low | observed `bearing_deg` returning exactly `360.0` for `(0,0) → (1e-9, -1e-300)`, violating the documented `[0, 360)` contract | `rem_euclid` rounds a tiny negative angle up to exactly `360.0` | **Remediated (2026-08-28)**: second modular reduction folds `360.0` back to `0.0` + regression test |
| COR-012 | Medium | `LatLon` fields were public, so literal construction bypassed the validation `new` performs — the invariant COR-002 claims was not enforced at the type boundary | validated-constructor pattern with public fields does not enforce its invariant | **Remediated (2026-08-28)**: fields privatized behind `lat()`/`lon()` accessors; construction now only possible through validation; rejection paths covered by `latlon_rejects_invalid_input` |
| COR-013 | High | two observations straddling the ±180° meridian (~111 m apart) produced a spatial estimate near longitude 0 — ~20,000 km away — demonstrated by the initially failing `spatial_estimate_handles_antimeridian_straddling` | `spatial_estimate` averaged longitudes as linear degree values, which is invalid across the antimeridian wrap | **Remediated (2026-08-28)**: weighted 3-D unit-vector (spherical) centroid with a degenerate-vector guard and domain-clamped `asin`; locality of the common path additionally asserted in the existing spatial test |
| COR-014 | High | `Observation::seen_at` could extend a value, but `EvidenceStore` rejected reinsertion of that same observation ID, so the canonical store could not persist a repeated sighting | the immutable value builder had no matching canonical-store update operation | **Remediated (2026-09-04)**: `EvidenceStore::record_observation_seen_at` atomically replaces only the extended observation value; missing IDs and time reversal fail without mutation |
| COR-015 | Medium | finite extreme inputs made `ble_distance_m` return `Some(inf)` or `Some(0.0)`, contradicting its positive-finite estimate invariant | input validation did not validate the representability of the computed result | **Remediated (2026-09-04)**: non-finite and non-positive computed distances now return `None`; direct and numeric-boundary regressions cover overflow, underflow, and tiny positive path-loss exponents |
| COR-016 | High | `tracking_snapshot` short-circuited the entire filtered-signal bundle when `rssi_spread_db` was NaN/negative/infinite; live repro showed filtered RSSI, trend, proximity, and freshness erased together with bounds/confidence. Separately, `signal_trend` treated non-finite inputs as `Stable` and `proximity_label` mapped `+inf`→`Immediate` / `NaN`→`Far` | distance-range and confidence failures used `?` on the whole snapshot; trend/proximity helpers had no finite-input gate | **Remediated (2026-09-09)**: `tracking_snapshot` now hard-requires only a finite filtered RSSI; distance, bounds, and confidence fail independently (`confidence_percent: Option<u8>`); `signal_trend`/`proximity_label` return `Option` for non-finite inputs; JNI keeps conservative ordinal sentinels (Far/Stable/`-1`). Locked by `tracking_snapshot_keeps_filtered_signal_when_spread_is_invalid`, `proximity_label_rejects_non_finite_rssi`, `tracking_snapshot_survives_invalid_spread`, and live `cargo xtask verify-jni-live` / `verify-android-live`. API break → `bleradar-core` 0.5.0 |
| COR-017 | Medium | `MainActivity` called `NativeRadar.ensureLoaded()` but never read `isAvailable()`/`loadError()`; when `libbleradar_jni.so` fails to load (non-`arm64-v8a` device, ABI mismatch) `BleScanEngine` degrades to raw RSSI with `NaN` distances and `PROXIMITY_FAR` while the status line still read "Scanning… N device(s) in range" | a failure path was rendered as ordinary success — the "silent fallback that turns failure into empty success" that `RUST_CONVERSION.md` forbids | **Remediated (2026-09-10, compile-verified; on-device observation pending, MIG-003)**: every status line now goes through `MainActivity.setStatus`, which appends `status_native_unavailable` (naming the load error class) whenever `NativeRadar.isAvailable()` is false; the string is present in the rebuilt APK's `resources.arsc`. Decision #57, REQ-ANDROID-003 |
| COR-018 | Medium | `RadarScanService.onStartCommand` returned `START_STICKY` but only re-posted the idle notification; after the process was killed and the service restarted, scanning stayed stopped until the user pressed Start again | the restart path carried no record of whether scanning was requested, so recovery was impossible by construction | **Remediated (2026-09-10, compile-verified; restart observation on device pending, MIG-003)**: `onStartCommand` is now reached only by a scan request — the activity's explicit `startForegroundService` after the permission grant, or the sticky restart after process death while scanning — and starts/resumes the scan itself; a user stop drops the started state (`stopForeground` + `stopSelf`) so it is never resurrected. Decision #57, REQ-ANDROID-002 |
| COR-019 | High | `MainActivity.onStart` called `startForegroundService` unconditionally and `onStartCommand` immediately called `startForeground(..., FOREGROUND_SERVICE_TYPE_CONNECTED_DEVICE)` — before any runtime permission had been granted on a fresh install | on Android 14+ (the app targets API 34) the `connectedDevice` foreground-service type requires one of the `BLUETOOTH_SCAN`/`BLUETOOTH_CONNECT`/`BLUETOOTH_ADVERTISE` runtime permissions (or a normal permission this manifest does not declare) to be held at promotion time, otherwise the platform throws `SecurityException` from `startForeground`; on first launch none is granted yet, so the service would crash before the user could grant anything | **Remediated (2026-09-10, compile-verified; first-launch observation on an API 34+ device pending, MIG-003)**: the activity now only binds on start and issues `startForegroundService` solely from the Start action after `hasRequiredPermissions` is true; `onStartCommand` re-checks the permissions and stops itself (`START_NOT_STICKY`) instead of promoting when a restart finds them revoked. Decision #57, REQ-ANDROID-002 |
| COR-020 | Medium | `coords::parse` decoded `"4RRH46RW+RH😷"` (and any non-ASCII character whose low byte matches a Plus Code letter, e.g. U+1036, U+1052) as a valid coordinate; found by the 2026-09-10 randomised falsification campaign | `olc_index` truncated the character with `as u8`, so a forged code passed the base-20 alphabet lookup and minted a plausible in-range point instead of `None` | **Remediated (2026-09-10)**: only ASCII reaches the alphabet lookup (`u8::try_from`); `parse_plus_code_rejects_non_ascii_homoglyph_digits`; `bleradar-core` 0.6.1 |
| COR-021 | Medium | `normalise(Coordinates, "100,2000")` returned the canonical `"100.000000,2000.000000"` while `coords::parse` rejected the same pair; `"1e308,0"` became `"inf,0.000000"` | the bare-decimal fast path checked only finiteness, bypassing the `LatLon::new` range gate every other notation funnels through | **Remediated (2026-09-10)**: the fast path applies `LatLon::new`; impossible pairs fall through unchanged; `normalise_coordinates_does_not_canonicalise_out_of_range_pairs` |
| COR-022 | Medium | `normalise(Domain, "www. www.Example.com.")` gave `"www.example.com"` once and `"example.com"` when normalised again — one host, two UIDs | the `www.` peel loop ran before the leading-whitespace trim, so a `www.` exposed by the trim survived the pass | **Remediated (2026-09-10)**: peel and trim iterate to a fixed point; `normalise_domain_is_a_fixed_point_when_whitespace_separates_www_labels` |
| COR-023 | Medium | `normalise(Url, "http://example.com\t/")` gave `"http://example.com\t"` once and `"http://example.com"` again; likewise whitespace before a trailing slash in the path and at the end of the last query parameter | host, path, and query key/value were not trimmed, so whitespace that ended up at the end of the output was removed only by the next pass's outer `trim()` | **Remediated (2026-09-10)**: host/path/query components are trimmed (invisible for any valid URL); `normalise_url_is_a_fixed_point_with_whitespace_before_a_trailing_slash`, `normalise_url_is_a_fixed_point_with_whitespace_inside_the_query` |
| COR-024 | High | `correlate` in both `website.rs` and `infrastructure.rs` summed temporal compatibility over retained supports in a `u16`: with ≥ 656 overlapping independent supports (26 observations per side from distinct sources, well inside the default 10,000-observation limit) debug builds panicked with `attempt to add with overflow` and the release profile the Android library ships with silently reported a mean of 3 instead of 100; the `u16` corroboration term overflowed at 6,555 supports and the `u16`-truncated divisor reached zero at 65,536; found by the 2026-09-10 random-operation campaign | per-explanation ranking arithmetic was written in `u16` with `as` narrowing on a count that is bounded only by the observation limit; a quadratic scan over all supports per retained pair in the same function made large correlations slow (110 ms for 625 pairs) | **Remediated (2026-09-10)**: one shared `corroborated_score`/`mean_temporal_compatibility` authority in `infrastructure.rs` (usize/u64 arithmetic, unit-tested at 656 and 65,536 supports) used by both engines; the high-base-rate check uses a precomputed pair set (625-pair correlation 110 ms → 9.7 ms website, 40 ms → 5.8 ms infrastructure); `ranking_arithmetic_survives_hundreds_of_independent_supports` in both suites; `bleradar-core` 0.6.2 |
| COR-025 | Medium | `correlate_all` in both engines returned `DuplicateCorrelation` and aborted after any earlier manual `correlate` of one pair, leaving the pairs it had already persisted in place, so an incremental observe → `correlate_all` → observe → `correlate_all` workflow failed on the second batch with partial state; a `ResourceLimit` mid-batch likewise left an arbitrary prefix persisted | the batch iterated `correlate` directly on `self` and treated only `NoComparableObservations` as skippable | **Remediated (2026-09-10)**: the batch runs on a clone and is swapped in only on success (the same all-or-nothing contract as `observe_snapshot`), skips pairs already correlated in either direction, and returns only the reports it persisted, so it is idempotent; `correlate_all_skips_pairs_already_correlated_in_either_direction_and_is_idempotent`, `correlate_all_leaves_the_engine_unchanged_when_the_correlation_limit_is_hit` in both suites |
| COR-026 | Low | An infrastructure correlation edge and its persisted `Relationship` provenance cited the same observation once per retained pair it took part in (52 observations → 1,352 cited ids at 676 supports), while the website engine cited each once | `infrastructure.rs` passed `supporting_observation_ids()` through unchanged; `website.rs` had its own private sort-and-dedup helper | **Remediated (2026-09-10)**: one shared `unique_observation_ids` in `infrastructure.rs` used by both engines; `edge_observation_ids_are_unique_and_sorted` |
| COR-027 | Medium | Loading the website engine took 21.0 s for 8,000 observations (2.6 ms per `observe`, 234 µs at 1,000), the infrastructure engine the same, and each OSINT execution 68 µs, all growing linearly with the store: the documented 10,000-observation capacity cost tens of seconds per load; measured by `examples/engine_load.rs` | atomicity was implemented at seven call sites by cloning the whole `EvidenceStore` (or the whole engine) per operation instead of once at the store | **Remediated (2026-09-10)**: `EvidenceStore::transaction` (undo journal, nested, panic-safe) is the single all-or-nothing mechanism and every engine uses it; 3.2 µs per `observe` at 8,000 records, flat with store size; four store tests plus the existing rollback tests and campaigns; `bleradar-core` 0.6.3 |
| COR-028 | High | `UpdateCheckService.loadReleaseManifest` called `InputStream.readAllBytes()`, an API 33 method, in an app whose `minSdkVersion` is 26: on Android 8–12 the update check that `MainActivity.onStart` issues on first launch throws `NoSuchMethodError` at that call, an `Error` the surrounding `catch (Exception)` never sees, and the process dies; found 2026-09-12 by running the SDK's standalone lint (`NewApi`) over the sources | `javac` compiles against `android-36`'s `android.jar`, which has the method, and `d8 --min-api 26` checks nothing about library API levels; no lint ran anywhere in the pipeline | **Remediated (2026-09-12)**: every asset read goes through `Streams.readAllBytes` (a plain loop); `verify-android-live` runs `lint --check NewApi` with the compiled classes on its classpath and fails on any finding (falsified: reintroducing the call fails the gate at `Streams.java:28`); decision #89 |
| COR-029 | High | `cargo xtask build-apk` never packaged `src/main/assets/`: the committed APK held zero `assets/` entries, so `getAssets().open("release_manifest.txt")` would throw `FileNotFoundException` on every device and the bundled release manifest that drives the whole update decision (decisions #81–#87) was never shipped — the check would log "Failed to load release manifest" and retry indefinitely; found 2026-09-12 when the new `assets/dashboard.html` entry gate failed on the unchanged pipeline | `aapt2 link` was invoked without `-A <assets dir>`, and the APK entry gate listed only the manifest, DEX, native library and `resources.arsc` | **Remediated (2026-09-12)**: `-A <app>/assets` in `build-apk`; `assets/release_manifest.txt` and `assets/dashboard.html` are `REQUIRED_APK_ENTRIES` (observed failing before the fix: "APK entry set missing required members: assets/dashboard.html, assets/release_manifest.txt"); APK regenerated; decision #89 |
| COR-030 | High | `UpdateCheckService` called `startForeground()` only for retry starts although every start arrives through `startForegroundService()` (MainActivity's daily check, `BootCompletedReceiver`, the retry alarm; minSdk 26), so the two paths that keep the service running — a download in flight and a restored download — left it un-promoted and the platform kills the process after its foreground timeout (`ForegroundServiceDidNotStartInTimeException` on API 31+, `RemoteServiceException` before); raised by Copilot's post-merge review of PR #22 and confirmed against every start path | promotion was keyed on the `is_retry` extra instead of on how the service is started | **Remediated (2026-09-12)**: promote first on every start and leave the foreground through one `finish()` on every exit (which also retries a verified artifact whose installer hand-off fails instead of clearing its retry state, and resets the in-flight guard when a receiver re-registration fails); decision #90 |
| COR-031 | Medium | `BleScanEngine.snapshot()`'s no-native fallback sorted on the live volatile `lastSeenUptimeMillis`, which the scan callback rewrites concurrently; a comparator that observes different keys between comparisons can make `TimSort` throw `Comparison method violates its general contract` — the hazard decision #86 removed from the native ranking path but not from the fallback; raised by the same review | the fallback comparator read the field directly instead of sampling it | **Remediated (2026-09-12)**: both ranking paths sample their key once per device into an identity map before sorting; decision #90 |
| COR-032 | Medium | `ApiHttpServer.handle` caught only `IOException`: a `RuntimeException` from a collaborator on a handler thread would leave the pool's worker and, on Android, end the whole app process (every uncaught exception on any thread is fatal there). Latent since the server was written and reachable in practice once scan control landed: `RadarScanService.requestStart` can meet the API 31+ `ForegroundServiceStartNotAllowedException` (an `IllegalStateException`) from `startForegroundService`, and any engine failure surfaces on the same thread. Reproduced on the host: with the boundary removed, `verify-api-live`'s scripted control that throws killed the worker and the request got no answer | the handler had no exception boundary between the collaborators and the socket | **Remediated (2026-09-13)**: `handle` answers `500` with the exception's class and logs it at `SEVERE` (every route does its work before writing its response in one call, so nothing partial reaches the client); `requestStart` reports the background refusal as `409` instead of throwing; the harness's sixth scripted start throws and the checks require `500` followed by a normal answer; decision #92 |

No legacy defect is marked fixed without reproducible evidence. Enhancement entries are explicitly labeled and are not represented as exact legacy behavior.
