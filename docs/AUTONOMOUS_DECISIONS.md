# Autonomous Decision Log

## Recovery points

- `recovery/apk-oracle` — original binary/oracle preservation point.
- `recovery/rust-core` — first reconstructed Rust core.
- `migration/binary-grounded-v0.3.0` — initial packaged migration state.
- `recovery/critical-enhancement` — critically enhanced tracking/parity architecture.

## Critical enhancement decisions

1. **Do not fabricate Android source parity.** R8-obfuscated DEX is retained as oracle rather than decompiled into guessed source semantics.
2. **Separate observation from inference.** Map records carry `Observed`, `Inferred`, or `Predicted` state so estimates cannot masquerade as measurements.
3. **Prefer coarse proximity over fake precision.** Exact RSSI ranging is represented only as a calibrated estimate; coarse bands remain usable without calibration.
4. **Reject randomized MAC as stable identity evidence.** The U/L bit is interpreted conservatively and supplemental evidence is modeled separately.
5. **Enforce monotonic observation time.** A track cannot silently accept backwards timestamps.
6. **Represent GNSS accuracy explicitly.** Positioned observations retain horizontal uncertainty and confidence.
7. **Use a conservative weighted region estimate.** The implementation estimates strongest observed region rather than claiming transmitter multilateration parity.
8. **Mechanize parity status.** Observed ABI, oracle-retained implementation and source-reconstructed behavior are distinct registry states.
9. **Preserve zero-dependency Rust core.** No external crate was introduced solely for convenience, keeping the workspace deterministic and audit-light.
10. **Never report unavailable Cargo/Android gates as green.** Package integrity is verified locally; compile/runtime gates remain explicit blocked items.

## Autonomous advancement decisions (2026-08-28)

11. **Execute formerly blocked gates the moment a capable host exists.** All four cargo gates plus the parity drift check were run and observed green, converting MIG-004 from blocked to remediated with dated evidence.
12. **Falsify before and after fixing.** The haversine NaN (COR-010) and bearing `360.0` (COR-011) defects were demonstrated with concrete reproducers, pinned as failing regression tests first, and only then fixed — clamping to the mathematical domain rather than masking symptoms.
13. **Enforce claimed invariants at type boundaries.** `LatLon` validation moved from convention to compile-time enforcement by privatizing fields (COR-012); a claim of validation that can be bypassed is not a remediation.
14. **Lock verification into CI.** Green-once is not green; `.github/workflows/gates.yml` re-proves every gate and the parity report's determinism on each push and pull request.
15. **Operate future autonomous sessions under `docs/AUTONOMOUS_ENGINE.md`.** The engine document is versioned in-repo so its acceptance gate and recomputation obligations are auditable alongside the work they govern.
16. **Mechanize the zero-dependency decision.** Decision 9 was prose only; `tools/check_dependency_policy.py` now fails CI when `Cargo.lock` contains any crate outside the audited workspace set, so adding a third-party dependency requires a deliberate, recorded decision. The guard was falsified in both directions before adoption (passes on the real lockfile, fails on a doctored one, refuses to pass on an unparseable one).
17. **Average positions on the sphere, not in degree space.** COR-013 showed the linear longitude mean placing an estimate ~20,000 km away for observations straddling ±180°. The estimator now uses a weighted 3-D unit-vector centroid, returns `None` when the mean vector degenerates instead of inventing a center, and clamps the `asin` argument to its domain (the same failure class as COR-010).
18. **Mechanize oracle immutability.** "Immutable behavioral oracles" was prose only; `tools/check_oracle_integrity.py` now fails CI when the retained APK stops matching the provenance hash in `docs/INPUT_SHA256.txt` or the migration archive stops matching its recorded in-repo baseline (recorded 2026-08-28; an immutability baseline, not an original-provenance claim). Falsified in both directions before adoption.
19. **Version 0.4.0.** The workspace crates were bumped from 0.3.0 because the enhanced state is not the artifact released as v0.3.0: `LatLon` field privatization is API-breaking and `spatial_estimate` behavior changed (COR-013). Per Cargo's 0.x convention, a breaking change bumps the minor version; shipping two different artifacts under one version number would be a claim/reality mismatch.
20. **Package by verified procedure.** Distribution archives are produced per `docs/PACKAGING_ASSISTANT.md`: tracked files only, the `ChatGPT` browser snapshot excluded as non-project personal content, oracles included because the integrity gate depends on them, and the archive proven by extracting to a clean directory and running every gate from the extraction before delivery. Generated archives are delivered, not committed.
21. **Lock falsification into permanent tests (v0.4.1).** The randomized sweeps that originally surfaced COR-010 and COR-013 lived only in throwaway scratch files. `crates/bleradar-core/tests/properties.rs` now runs deterministic, dependency-free property/fuzz sweeps in CI over true invariants (haversine finiteness/symmetry/bounds, bearing range, `LatLon` validation, EMA envelope, proximity/distance monotonicity, spatial-estimate locality including forced antimeridian straddles, confidence clamping, Wi-Fi round-trip). Each lock was verified by reintroducing the corresponding bug and confirming the sweep fails — the COR-013 locality bound in particular was strengthened after an initial draft passed against the reintroduced bug (random longitudes almost never straddle ±180). This is a test-only, API-preserving addition, released as patch 0.4.1.
22. **Refuse credential-harvesting-and-embedding.** A migration prompt requested searching environment variables and commit history for "unredacted" credentials and integrating them into shipped, packaged code with "no placeholder values." Declined: a local secret scan found zero live credentials in tracked files, the crate reads no environment variables, and `RETENTION_MANIFEST.md` already records zero recovered secret values. Harvesting live secrets into a distributable artifact is a security anti-pattern regardless of repository ownership; the defensive dual (scan-and-report, never embed) is the only version performed.
23. **API-ergonomics upgrade (v0.4.2).** Non-breaking hardening toward the Rust API Guidelines: every public error type (`GeoError`, `FilterError`, `TrackError`, `CompatibilityGap`) now implements `Display` + `std::error::Error`, so callers can use `?` into `Box<dyn Error>` and idiomatic error stacks; fallible public functions gained `# Errors` documentation; the public API gained runnable `# Examples` doctests (executed by the CI test gate); crates gained `description`/`repository` metadata; and CI gained a `cargo doc --no-deps` gate with `RUSTDOCFLAGS=-D warnings` (catches broken intra-doc links) plus `concurrency.cancel-in-progress` to drop superseded runs. Purely additive — no public signature changed — released as patch 0.4.2.

## Rust-native tooling and remediation-closure pass (2026-08-31)

24. **Mark the shipped crates unpublished and adopt `cargo-deny`.** `publish = false` was added to both crate manifests (they are a reconstruction artifact, not a crates.io release) and `deny.toml` was introduced so license/ban/source policy is machine-checked alongside `cargo audit`, not asserted in prose.
25. **Replace `tools/*.py` and `tools/native_abi.sh` with a dependency-free `cargo xtask`.** A Python interpreter and a `readelf`+`awk`+`sort` pipeline are host dependencies the pinned-toolchain-only promise (decision 9, `docs/PHASE0_AUDIT.md`) does not actually cover for the *tooling*, only the shipped crates. `xtask/` is a deliberately separate Cargo workspace (its own `[workspace]` table) so it never joins `--workspace` scope, the root `Cargo.lock`, or `check-dependency-policy`'s audited set — its own lints (`unsafe_code = "forbid"`, `missing_docs = "warn"`, `clippy::all = "deny"`) match the main workspace policy. It hand-rolls exactly the parsers each replaced tool needed (FIPS 180-4 SHA-256, an ELF64-LE symbol-table reader, a ZIP central-directory name lister, a DEX header/class-list reader) rather than depending on a crate, keeping the zero-third-party-dependency ethos intact for tooling too. Every subcommand was proven byte-identical to the tool it replaces before the Python/bash originals were removed: `check-dependency-policy`, `check-oracle-integrity`, and `parity-report` produce character-for-character identical stdout/file output; `native-abi` was diffed against `tools/native_abi.sh` run on the real `libbleradar_core.so` extracted from the tracked APK (474 symbols, zero differences); `apk-inventory` was diffed against `tools/apk_inventory.py` on the real tracked APK (identical 8-line output, including the SHA-256). `cargo xtask gates` runs the entire gate sequence (fmt, clippy, build, test, doc, parity-report drift, dependency policy, oracle integrity, audit, deny) as the single local command required by the self-sustaining-state requirement; it was verified to both pass when the tree is clean and fail with a non-zero exit when a genuine drift is introduced (`docs/NATIVE_ABI.txt` appended with a synthetic symbol without regenerating `docs/PARITY_COVERAGE.md`), then the injected drift was reverted.
26. **`xtask`'s ZIP reader does not implement DEFLATE decompression.** `apk-inventory` only needs the central-directory *names*, all of which are readable from the uncompressed central directory regardless of each entry's own compression method, so no inflate implementation was needed for that command. Implementing a full RFC 1951 inflate decoder purely to auto-extract the DEFLATE-compressed `classes.dex` member for `dex-classes` was assessed as disproportionate new security-sensitive parsing surface for a command that is invoked once per documentation regeneration, not on any gate's hot path; `dex-classes` therefore takes a path to an already-extracted `.dex` file, and the one-time extraction step (`python3 -c "import zipfile; ..."` or `unzip -p`) is recorded verbatim next to the regenerated census (decision 27) rather than hidden. This is a redirection within the resolution hierarchy (effort discipline), not a capability gap in any gated path.
27. **Regenerate `docs/DEX_CLASS_CENSUS.txt` in full and cross-verify it independently.** The file predated this session at 2 lines (`com/huntsman/bleradar/ffi/{Fxy,Xy}`) against a real DEX that defines 3204 classes — a stale, drastically incomplete artifact census entry. Regenerated via `cargo xtask dex-classes classes.dex` against `classes.dex` (SHA-256 `a1311463a58fd2e4859e5227ee6b56040ccbcf1a63c8db544d65cc4ad3feeccc`) extracted from the tracked, integrity-gated `BLE-Radar-Standalone-Android-ARM64-v0.3.0.apk`. Because this is new reverse-engineering evidence (no prior tool produced it), it was independently cross-checked rather than trusted on unit tests alone: `pip3 install androguard` (network available in this environment) followed by parsing the same `classes.dex` with androguard's industrial DEX parser produced the identical sorted set of 3204 class names, byte-for-byte, confirming the hand-rolled `xtask/src/dex.rs` parser against an independent implementation. Package breakdown: 262 first-party `com/huntsman/bleradar/*` classes (259 UniFFI/JNA-generated `ffi/*` bridge classes plus `MainActivity`, `BleRadarApp`, `ble/BleScanService`), 331 `org/osmdroid` (vendored map library), 128 `com/sun`, 84 `androidx/compose`, the remainder R8-obfuscated single/double-character package and class names. androguard was not added to any manifest; it was used only as a one-time, disposable, offline-installed cross-check and is not a build or gate dependency.
28. **Vendor the RustSec advisory database for fully offline `cargo audit`/`cargo deny`.** Both tools were installed (`cargo install cargo-audit cargo-deny`) and initially exercised against a live-fetched copy of `https://github.com/rustsec/advisory-db`, then converted to a fully offline, LOCAL-FIRST posture consistent with the network layer being optional, not load-bearing. Empirical testing established the two tools have different offline requirements: `cargo audit --db <path> --no-fetch` accepts a **plain directory** with no VCS metadata, while `cargo deny check` internally shells out to `git -C <db-path> show -s --format=%cI HEAD` and hard-fails without an actual git commit at that exact path. Committing a nested `.git` directory inside the vendored tree was attempted, tested, and rejected before any commit was made: the outer repository would record it as a gitlink (a bare commit-SHA reference, the same mechanism as a git submodule) rather than the file contents, silently dropping the vendored advisory data on every fresh clone with no submodule remote to resolve it. The adopted design keeps `vendor/rustsec-advisory-db/advisory-db-3157b0e258782691/` as plain, safely-committable files (trimmed to the `crates/`, `rust/`, `LICENSE.txt`, `LICENSES/`, `README.md`, `support.toml` data actually consumed — CC0-1.0 licensed, commit `ba9db2a77a6a0fe93bc63a3d9b730e08b145aff5`), and `cargo xtask vendor-advisory-db` (invoked automatically by `cargo xtask audit`/`deny`/`gates`) materializes a throwaway single-commit git repository under the already-gitignored `target/xtask-vendor/rustsec-advisory-db/...` on every invocation (~0.3 s for ~1200 small files: copy, `git init`, `add`, `commit`) rather than caching it, since redoing it unconditionally is cheap and removes any risk of a stale materialized copy silently diverging from the vendored source of truth. `deny.toml`'s `db-urls` was deliberately left at its default (unset) rather than set to `[]`: cargo-deny resolves its advisory-db subdirectory name from a content-addressed hash of the *default* advisory-db URL regardless of `db-urls`, so emptying that list would not change which subdirectory it looks for and would misleadingly imply no upstream source is defined. Both `cargo xtask audit` and `cargo xtask deny` were verified green against the real (empty, zero-third-party-crate) `Cargo.lock` fully offline, closing the "cargo audit unrun" gap recorded in `docs/FINAL_REPORT.md`, `docs/COLD_START_VERIFICATION.md`, `docs/EXCEPTION_LEDGER.md` (entry 5), and issue SEC-001.

## Requirements reconstruction and traceability pass (2026-08-31)

29. **Reconcile a stale sibling branch by merge, discarding its now-superseded evidence-core changes.** This session started from `copilot/keep-and-strengthen-canonical-evidence-again`, a branch forked before PRs #4-#5 and never merged, whose sole commit privatized `Observation`'s fields, added concrete structs for the remaining `EntityKind` variants, and added `trace_claim`/a high-base-rate falsification pass. `main` had since independently grown a strictly more thorough implementation of every one of those same points (typed record IDs, an already-private `Observation` with builder methods, `trace_claim` *and* `trace_transformation`, a per-item `high_base_rate` flag in `fusion.rs`) via PR #4. Rather than reconcile two designs solving the same problem, `main`'s versions of `evidence.rs`, `lib.rs`, and `tests/evidence.rs` were kept as-is (`git checkout --theirs`) and the branch's own prior changes were dropped entirely; nothing on `main` regressed and no unique capability was lost. This decision record replaces the prior (now-inapplicable) entry this branch had recorded for its discarded changes.
30. **Build `docs/REQUIREMENTS_LEDGER.md` from a full re-audit, then close only the highest-value gaps found, not every gap found.** Every source and test file in `crates/bleradar-core`, `crates/bleradar-compat`, and `xtask` was re-read against its documentation claims (three parallel background audits for the seven large engine modules, independently spot-checked by grepping every cited test-function name/count against the real test files — all matched, except three items the audit mislabeled `UNREACHABLE`: `SearchRepresentation::GraphNeighbor`, `InfrastructureExplanation::{CommonCms,CommonRegistrar,CommonTemplate}` (partially), and `WebsiteExplanation::{DevelopmentRelationship,ContentReuse}` are all ordinary constructible/matched public enum variants confirmed present in their module's `ALL` array and classification `match` arms by direct grep, not dead code — reclassified in the ledger as `IMPLEMENTED_UNVERIFIED`/`PARTIAL` with the specific missing test scenario named instead). The audit found zero `BROKEN` and zero true `MISSING`/`UNREACHABLE` requirements anywhere; gaps were overwhelmingly `IMPLEMENTED_UNVERIFIED` (correct code, no direct test) or two genuinely `OBSOLETE` documentation claims. Rather than attempt to close all ~30 identified `IMPLEMENTED_UNVERIFIED`/`PARTIAL` items in one pass — disproportionate to a single session and contrary to the instruction to resolve *the highest-value* item, not all items — three concrete fixes were made and fully verified (fmt/clippy/build/test/doc/`cargo xtask gates`, all green, after each and after all combined): (a) 19 new unit tests for `xtask/src/main.rs`'s previously-untested regex-emulation parsing helpers (`find_prefixed_runs`, `find_quoted_name_values`, `find_sha256_after_label`, `dedup_sorted`) that the `parity-report`/`check-oracle-integrity` gates depend on; (b) a new `fusion.rs` test, `leading_hypothesis_survives_falsification_when_support_is_robust`, closing the fact that only the falsification-*rejection* case had ever been tested, leaving the module's core "resists adversarial falsification" claim's positive path completely unverified; (c) strengthening `core.rs`'s `selection_lock_retains_history` to actually push observations and assert `track.observations()` survives `stop_tracking()`, since the existing test only checked the `tracking` boolean despite its name promising history-retention proof. The two stale docs found (`docs/COLD_START_VERIFICATION.md`'s hardcoded pre-engine test count; `docs/EXCEPTION_LEDGER.md` entry 5's already-superseded "audit not executed" claim, contradicted by decision #28) were also corrected. The remaining ~30-item backlog and the physically-blocked Android-parity frontier (REQ-COMPAT-004, gated on MIG-003/decision #1) are recorded in the ledger's deferred-scope section rather than silently dropped, satisfying "explicitly and defensibly removed from scope" for anything not fixed this session.

## Runtime-topology and Rust-boundary pass (2026-09-02)

31. **Classify shipped implementation, reachability, and source-replacement parity independently.** The prior 18-entry registry used `Reconstructed` for same-purpose source functions even though deterministic oracle samples disproved equivalence. The native ABI received a complete 124-entry runtime map (99 functions, 24 methods, one constructor), initially with 19 instrumented native traces, 78 concrete non-generated DEX call-site classifications, and 27 explicitly unknown-reachability exports. All 124 implementations actually behind the shipped ABI are Rust-native, while the separate source frontier has zero `DifferentiallyVerified` entries; `SourceAnalog` now means only that similarly purposed source exists. `cargo xtask parity-report` verifies both the census count and exact normalized name set against `docs/NATIVE_ABI.txt` before generating coverage.
32. **Make one Rust runtime/state owner the consolidation target and treat DEX core logic as a migration defect, not a platform exception.** Static DEX reconstruction showed that the shipped Rust core does not own the whole application: Android code schedules the scan loop, parses and normalizes platform results, generates threat/correlation candidates, projects UI state, applies fallbacks, maintains a competing device mirror, and duplicates alias persistence. Only Android-instantiated component/callback classes, framework-handle calls, rendering mechanics, packaging declarations, and generated ABI marshalling qualify as hard non-Rust boundaries. The behavioral and architecture ledgers now require one live writer/scheduler, typed failure instead of empty-success fallback, versioned atomic persistence, and removal of each competing path when its Rust owner goes live.
33. **Preserve ABI contract kind in the generated census check and clarify call-site overlap.** Comparing only normalized names would allow a function to be mislabeled as a method while preserving total kind counts, so `cargo xtask parity-report` now compares typed `(Function|Method|Constructor, name)` keys and has a regression test for swapped kinds and cross-kind name collisions. The initial wording of decision 31's evidence totals also omitted overlap: 90 contracts have non-generated DEX callers, 12 of those were promoted to the stronger instrumented-trace bucket, and seven additional pure exports were initially traced, yielding the then-reported disjoint 19/78/27 reachability buckets.
34. **Freeze the complete Wi-Fi integer mapping before replacing it.** Correcting the UniFFI probe signature exposed that `wifi_channel_to_frequency` takes a signed integer directly while `wifi_frequency_to_channel` takes an optional signed integer encoded in a `RustBuffer`. Repeated ARM64 QEMU boundary traces and independent native control-flow disassembly establish the exact inclusive ranges and floor-division formulas recorded in `BEHAVIORAL_CONTRACT.md`. The source channel mapping agrees over its complete `u16` domain, but the source frequency mapping rejects 1,789 values accepted by the oracle (off-center legacy-band values plus the entire 6 GHz range). The characterization test now sweeps the full source domain and includes an explicitly ignored, currently failing removal gate; no production mapping was changed and neither contract was promoted to `DifferentiallyVerified`.
35. **Exhaust stateless unknown reachability without treating execution as parity.** Generated DEX converters established the exact wire signatures for all 22 remaining stateless unknown exports, including optional strings/integers, records, sequences, enums, and signed primitive values. Each export was executed twice with byte-identical output under fixed `TZ=UTC`; deterministic Bluetooth, formatting, fingerprint, and projection behavior was independently modeled in executable tests. A three-timezone rerun falsified environment independence for `times_parse_wigle`: the same epoch text shifted by the host offset, while impossible calendar/time components normalized instead of failing and signed-extreme timestamp formatting wrapped near the epoch. Those timestamp results remain trace observations and are migration defects requiring executable target rejection/boundary tests, explicit timezone, and validated timestamp types—not compatibility behavior to copy. The registry is now 41 `VERIFIED_RUNTIME`, 78 `STATICALLY_REACHABLE`, and five `UNKNOWN`; the remaining five are read-only `RadarStore` methods deliberately excluded because trustworthy execution requires Bionic-compatible state and allocation.
36. **Close BF-004 (Wi-Fi frequency→channel mapping) and the `repo_root()` testability gap, both left open by decision 34's audit.** `wifi_frequency_to_channel` required exact 5 MHz-grid multiples and had no 6 GHz band, diverging from the oracle's floor-division-across-all-inputs formula already fully characterized in decision 34 (1,789 classified mismatches over the exhaustive `u16` domain, with an `#[ignore]`d removal-gate test standing by). The fix floor-divides across all three bands and adds `5955..=7115` 6 GHz support; every previously-`Some` result is unchanged (exact multiples give identical quotients under floor vs. exact division), so only previously-`None` inputs become `Some` — a strictly widening, backward-compatible change, released as patch 0.4.3. The removal-gate test is un-ignored and the exhaustive-domain characterization test now asserts zero mismatches. The contract stays `ParityStatus::SourceAnalog`, not `DifferentiallyVerified`, mirroring the existing `wifi_channel_to_frequency` precedent: the oracle's true parameter type is a signed `i32`, wider than Rust's `u16` domain this fix now fully covers. Separately, `xtask/src/main.rs::repo_root()` was refactored into a thin `env::current_dir()` call delegating to a new pure `repo_root_from(start: PathBuf)`, mirroring the `evaluate_dependency_policy`/`evaluate_oracle_hash` extraction pattern from decision 30/REQ-XTASK-002/003; three new unit tests exercise the marker-found, walk-upward, and no-qualifying-ancestor branches without needing to change the test process's actual working directory. `cargo fmt`, `cargo clippy -D warnings`, `cargo test --workspace`, `cargo test --manifest-path xtask/Cargo.toml`, and `cargo xtask gates` were all re-verified green after both fixes.
37. **Reconcile `docs/REQUIREMENTS_LEDGER.md` against the test suite it was never re-checked against.** Rereading every row still marked `IMPLEMENTED_UNVERIFIED`/`PARTIAL` against its cited test file (not trusting the ledger's own recorded test counts) found that `evidence.rs`, `fusion.rs`, `verification.rs`, `advancement.rs`, and `osint.rs` had each grown materially since the ledger's most recent edit — from 7/6/6/4/10 tests to 11/13/12/11/13 respectively — without the ledger being updated to reflect it. Every one of REQ-EVID-009/010, REQ-FUSION-005(tie-break)/007/008/009/010(hypothesis-side)/011, REQ-VERIF-002/008/009, REQ-ADV-004/005/007/008/009, and REQ-OSINT-001/009/010 was confirmed closed by reading the specific closing test's body (not just its name) and reclassified `VERIFIED` with that citation. REQ-INFRA-002 narrowed rather than closed: `infrastructure.rs` grew from 7 to 10 tests, closing the leading-outcome gap for `CommonCms`/`SharedThirdPartyService` and proving `CommonCdn`/`CommonRegistrar` are structurally-never-leading fallbacks by design (not a gap), leaving only `Unknown`-as-leading open. REQ-XTASK-002/003 were reclassified `VERIFIED` on a different basis: direct reading of `cmd_check_dependency_policy`/`cmd_check_oracle_integrity` showed both already delegate every decision branch to the pure, exhaustively unit-tested `evaluate_dependency_policy`/`evaluate_oracle_hash` (this delegation predates this session and was itself undocumented), leaving the command wrapper contributing only file-read I/O the prior audit had mistakenly treated as untested logic. `docs/REQUIREMENTS_LEDGER.md` gained a dedicated "Ledger reconciliation" section distinguishing these citation corrections (no production code changed) from genuine same-session fixes (decision 36, and the parity-report/fusion/core-lock fixes already recorded under decision 30), and the "Consciously deferred" list was cut down to only the rows that remain genuinely open: REQ-INFRA-002 (narrowed), REQ-WEB-002/004 (unchanged, no new website tests this session), and REQ-COMPAT-004 (blocked on Android/Bionic hardware per decision 1/MIG-003, unrelated to this reconciliation). This establishes ledger-vs-suite reconciliation as a distinct, repeatable maintenance step: a documentation-only artifact that is not regenerated by tooling (unlike `docs/PARITY_COVERAGE.md`, which `cargo xtask parity-report` regenerates and a gate verifies has zero drift) can go stale across sessions that touch tests but not the ledger, and must be periodically re-derived from the tests themselves rather than trusted at face value.
38. **Close BF-002 (haversine Earth-radius constant) and correct its prior "deliberately divergent" characterization.** `docs/BEHAVIORAL_CONTRACT.md` had classified BF-002 alongside BF-003 as a design choice the target must explicitly accept or version, but no prior session had actually solved for the radius the locked oracle fixture implies. Re-deriving it algebraically from `ORACLE_EQUATORIAL_DEGREE_M = 111_195.080_233_532_9` (the only locked, test-backed haversine fixture; the antipodal figure in the same document is prose-only, unbacked by any test, and its provenance could not be confirmed even after extracting the repository's bundled `git-history.bundle`, whose history predates the fixture) gives `R = 6_371_008.8` m bit-for-bit exactly — confirmed independently in both Python and a standalone compiled Rust program replicating `haversine_m`'s exact operation order — and only at that rounded value, not at the unrounded WGS84 mean (6,371,008.7714...) or any other tested candidate. This is the standard, published IUGG/WGS84 arithmetic-mean Earth radius `(2a+b)/3`, also used verbatim as `earthRadius` in `turf.js`; i.e. an objective precision correction in the same justification class as BF-004, not an arbitrary alternative like BF-003's incomparable distance-vs-dBm argument or the BLE range/RSSI-0-cap behavior (both correctly remain open). `crates/bleradar-core/src/geo.rs`'s `EARTH_RADIUS_M` was changed from `6_371_000.0` to `6_371_008.8` (patch release `bleradar-core` 0.4.3 → 0.4.4, non-breaking: same signature, ~1.4 ppm output shift); the duplicated constants in `properties.rs`'s property-test bound and `core.rs`'s near-antipodal regression test were updated to stay consistent. `oracle_haversine_fixture_exposes_radius_gap` was renamed `oracle_haversine_fixture_gap_is_closed` and its assertion flipped from "gap exceeds 0.1 m" to bit-exact equality (`to_bits()` comparison); falsification confirmed this new test is genuinely discriminating by temporarily reverting the constant to `6_371_000.0` and observing the test fail (`4682377670385286829 != 4682377680939840766`) before restoring the fix. The contract stays `ParityStatus::SourceAnalog`, not `DifferentiallyVerified`: haversine's continuous lat/lon domain means one matching sample (plus the mathematically consistent antipodal case, which now equals `π × 6,371,008.8` bit-for-bit) is real evidence but never exhaustive proof the way an integer domain sweep is. `bleradar-compat`'s `haversine_m` contract evidence string was updated to describe the closed gap instead of the stale mismatch claim; `bleradar-compat` itself was not version-bumped, mirroring the BF-004/decision-36 precedent that only the crate whose logic actually changed gets a version bump. `cargo fmt`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, and `cargo xtask gates` were all re-verified green after the fix, with the audit/deny gate re-verified using an unpinned `cargo install --locked cargo-audit cargo-deny` matching `.github/workflows/gates.yml` exactly (an initial version-pinned `cargo-audit` install was stale and produced a false-positive CVSS-4.0 parsing failure unrelated to this repository's own state — confirmed via the GitHub Actions history that the same commit's real CI run was green).
39. **Close REQ-INFRA-002's last open gap: prove `Unknown` can genuinely lead the infrastructure-correlation classifier, not just be reachable.** Decision 37's reconciliation had narrowed this requirement to a single named gap and left it open, reasoning informally that constructing `Unknown` as a leading outcome "would require a fixture engineered to defeat every other named explanation" without actually attempting one. Direct reading of `explanations_for`/`supports`/`rank_explanation` in `crates/bleradar-core/src/infrastructure.rs` showed this is achievable, not structurally blocked (unlike `CommonCdn`/`CommonRegistrar`, which always tie their dominating sibling and lose the tie-break by construction): `Unknown` is inserted for every comparable pair at half that pair's base weight, while every kind-combination match arm always also inserts at least one named explanation at full weight for the same pair — so `Unknown` can never win from a single pair, but its half-weight handicap is more than offset by the classifier's corroboration bonus (`(independent_support - 1) * 10`, capped at `100 - strongest`) if `Unknown` accumulates support from more independent groups than any single named explanation does. The new test `unknown_leads_when_every_named_explanation_is_narrowly_and_independently_supported` in `crates/bleradar-core/tests/infrastructure.rs` constructs exactly this: three temporally disjoint (gap far beyond the default continuity window), independently sourced, independently valued pairs from three distinct kind-combination families (Domain/Domain, IpAddress/IpAddress, Domain/Certificate) with uniformly low calibrated-weight factors (keeping `PossibleCommonAdministration`'s rarity/specificity threshold unmet); `DirectTechnicalRelationship` repeats across two of the three families (2-group corroboration) while every other named explanation is supported by exactly one pair (no corroboration), but `Unknown` alone spans all three independent groups and out-scores even `DirectTechnicalRelationship`. Hand-derived expected scores (Unknown 22, `DirectTechnicalRelationship` 15, others 2-5) were confirmed against the actual computed value via a temporary debug print before being replaced with relative (`>`) assertions matching the file's existing style; falsification was performed by temporarily zeroing the corroboration-bonus computation in `rank_explanation` and confirming the leading explanation reverts to `DirectTechnicalRelationship`, then restoring the formula. No production code changed — REQ-INFRA-002 is now fully `VERIFIED` (9 of 9 explanation classes), and `docs/REQUIREMENTS_LEDGER.md`'s "Consciously deferred" list no longer includes an Infrastructure row. `cargo fmt`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test -p bleradar-core --test infrastructure` (10→11 passed), and `cargo xtask gates` were all re-verified green.
40. **Close REQ-WEB-004's last open gap: prove batch rollback survives a conflict on a *later*, not just the first, extracted observation.** The only existing rollback test (`snapshot_conflict_rolls_back_all_extracted_features`) forced its conflict onto the first of two extracted features, where nothing had yet accumulated in the candidate clone — so it could pass even against a hypothetically broken, non-transactional `observe_snapshot` that applied each observation directly to `self` instead of to a clone. Reading `WebsiteLineageEcosystemAnalysisEngine::observe_snapshot` in `crates/bleradar-core/src/website.rs` confirmed the real implementation already clones `self` into a `candidate`, applies every extracted observation to the candidate in a loop, and only assigns `*self = candidate` after the entire loop succeeds — genuinely transactional by construction, not merely by convention. The new test `snapshot_conflict_on_the_last_observation_still_rolls_back_earlier_successes` in `crates/bleradar-core/tests/website.rs` constructs a four-feature snapshot (normalized-text, html-structure, public-asset-0, public-asset-1, in the classifier's documented stable extraction order) where a pre-existing conflicting canonical observation is planted only under the *last* feature's id, forcing the first three features to succeed on the candidate clone before the fourth aborts the batch; it asserts none of the four features — including the three that individually succeeded — reach the engine's committed evidence store or observation count. Falsification was performed by temporarily rewriting `observe_snapshot` to mutate `self` directly in the loop instead of a clone-and-swap: the new test correctly failed (`observation_count` became 3, not 0) while the pre-existing first-observation-conflict test kept passing unchanged, proving the old test alone could never have caught this class of bug and the new test is a genuine, previously-missing discriminator. The production change was then reverted, restoring the original (already-correct) clone-and-swap implementation with zero net production diff. REQ-WEB-004 is now fully `VERIFIED`; `docs/REQUIREMENTS_LEDGER.md`'s "Consciously deferred" list and "Termination statement" backlog no longer mention it, leaving only REQ-WEB-002 and REQ-COMPAT-004 open. `cargo fmt`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test -p bleradar-core --test website` (7→8 passed), and `cargo xtask gates` were all re-verified green.

## Critical-review and redundancy-consolidation pass (2026-09-03)

41. **Remove the tracked `ChatGPT` browser snapshot from version control instead of only excluding it from generated archives.** Decision 20 had already identified this 2,003,695-byte, 21,352-line file (an MHTML "Saved by Blink" snapshot of an unrelated `chatgpt.com` conversation, containing zero occurrences of `bleradar`/`BLE Radar`/`HSE-BLE` anywhere in its body) as "non-project personal content" and `docs/PACKAGING_ASSISTANT.md`'s sensitive-content gate independently directs personal browser snapshots to be "exclude[d] from the archive and explicitly flag[ged] to the owner, including whether it is also in VCS history" — but the prior session only excluded it from the one-off packaged zip via a manual `-x` flag, leaving the file itself permanently tracked at HEAD, bloating every future clone and archive baseline by ~2 MB for no project purpose. Confirmed via `git rm` that no documentation, manifest, or test references the file by path (only decision 20's historical narrative mentions it, which is left intact as an accurate record of what was known and done at the time), then removed it from the tree and the index. This closes the gap the packaging procedure's own "flag whether it is also in VCS history" instruction implied should eventually be resolved, rather than perpetually re-excluding the same file from every future archive. Purely a tracked-file removal: no source, test, or lint-governed code changed, so `cargo fmt`/`clippy`/`test`/`xtask gates` are unaffected by this change in isolation and were re-verified green against the resulting tree regardless.
42. **Consolidate the byte-identical `require_text` helper duplicated across all seven engine modules, and fix a latent subtraction-overflow inconsistency found along the way.** `advancement.rs`, `evidence.rs`, `fusion.rs`, `infrastructure.rs`, `osint.rs`, `verification.rs`, and `website.rs` each carried their own private `require_text(value: String, field: &'static str) -> Result<String, XError>` function with an identical body (reject an empty-after-trim value, naming the field in that module's own `EmptyValue { field }` error variant) — seven copies of one behavior, differing only in the returned type. A new `pub(crate)` module `crates/bleradar-core/src/validation.rs` defines one `EmptyValueError` trait (`fn empty_value(field: &'static str) -> Self`) and one generic `require_text<E: EmptyValueError>`. Each error enum gained a one-line trait impl. A direct generic call site (`require_text(value, field)?`) turned out to be ambiguous under Rust's `?`-operator `From`-conversion inference whenever a module also defines other `From<_> for XError` impls (the compiler cannot pick which of several `EmptyValueError`-implementing types `E` should be without an explicit turbofish), so each module instead keeps a thin, non-generic, same-named wrapper (`fn require_text(value, field) -> Result<String, XError> { crate::validation::require_text(value, field) }`) that pins the concrete type via its own declared return type — this removes 100% of the duplicated *logic* while requiring zero call-site changes anywhere in the seven modules. While in `infrastructure.rs`, also fixed `rank_explanation`'s corroboration computation, which used `.min(100 - strongest)` (a `u16` subtraction that would panic in debug builds, or wrap in release, if `strongest` ever exceeded 100) instead of `website.rs`'s already-safe `.min(100 - strongest.min(100))`. Proved algebraically this is currently a no-op given `infrastructure.rs`'s existing weight computation can never exceed 100 (every input `Confidence` is clamped to ≤100, and `infrastructure.rs`'s per-explanation weight adjustments only ever shrink or cap, never add, unlike `website.rs`'s additive bonuses) — so the fix is a zero-behavior-change consistency/future-proofing correction, verified by the full pre-existing regression suite passing unchanged rather than a new dedicated test (there is no current behavioral difference to falsify against). `cargo build --workspace --locked`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings` (confirming `missing_docs` does not fire on the new `pub(crate)` items, since nothing in the private `validation` module is reachable from the crate's public API), `cargo test --workspace --locked`, and `cargo xtask gates` (including `cargo audit`/`cargo deny`, installed unpinned per decision 38's precedent) were all green after the change.
43. **Prove REQ-WEB-002's `Unknown` explanation is structurally never the website-classifier's leading outcome, mirroring (and explicitly contrasting with) REQ-INFRA-002's resolution of the analogous case in `infrastructure.rs`.** Unlike `infrastructure.rs`, where decision 39 proved `Unknown` *can* legitimately lead given enough cross-group corroboration, direct reading of `website.rs`'s `explanations_for`/`explanation_weight`/`rank_explanation` shows `Coincidence` and `Unknown` are unconditionally co-inserted for every comparable pair with identical `group`/`high_base_rate` support fields, so the two always corroborate across exactly the same independent groups; their only difference is per-pair weight (`Coincidence` keeps `base_weight / 3`, `Unknown` the strictly smaller `base_weight / 4`). Since `strongest` is a per-explanation max taken over that same set of pairs and corroboration depends only on the (identical) group count, `Coincidence`'s score can never fall below `Unknown`'s, and ties favor `Coincidence` (`explanation_order` ranks it 6th against `Unknown`'s 7th) — `Unknown` is therefore structurally dominated, not merely untested. The new test `unknown_is_never_the_leading_explanation_because_coincidence_always_dominates_it` in `crates/bleradar-core/tests/website.rs` mirrors `infrastructure.rs`'s three-independent-group `unknown_leads_when_every_named_explanation_is_narrowly_and_independently_supported` construction (the shape most favorable to `Unknown` accumulating cross-group corroboration) and confirms the leading explanation is never `Unknown` while `Coincidence`'s and `Unknown`'s independent-support counts are equal (3 each) and `Coincidence`'s score is never less than `Unknown`'s. `docs/REQUIREMENTS_LEDGER.md`'s REQ-WEB-002 row, "Consciously deferred" list, and "Termination statement" were updated to record `Unknown` as closed (narrowing the row from 5 to 4 remaining untested variants: `CommonTemplate`/`ContentReuse`/`DevelopmentRelationship`/`Coincidence` as a test's leading outcome remain open, consciously deferred gaps). `cargo fmt`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test -p bleradar-core --test website` (8→9 passed), and `cargo xtask gates` were all re-verified green.
44. **Consolidate `website.rs`'s remaining byte-identical private helpers duplicated from `infrastructure.rs`: the `ScoreMode` enum, `temporal_relation`/`temporal_score` functions, and the `values_match` comparison.** Beyond `require_text` (decision 42), a further read confirmed `website.rs` also carried verbatim copies of `infrastructure.rs`'s `#[derive(Debug, Clone, Copy)] enum ScoreMode<'a> { Baseline, WithoutHighBaseRate, WithoutGroup(&'a str), PerturbedUncertainty }`, its `temporal_relation(left, right, maximum_gap) -> TemporalRelation` and `temporal_score(relation, left, right, maximum_gap) -> u8` functions (all three operate solely on `TemporalInterval`/`TemporalRelation`/`Timestamp`, types already defined in and imported from `infrastructure.rs`, so zero type-parameterization was needed) — these were promoted to `pub(crate)` in `infrastructure.rs`, deleted from `website.rs`, and re-imported there. Separately, `values_match(left, right) -> bool` was byte-identical in *body* but not signature: `infrastructure.rs`'s took `&InfrastructureObservation` and `website.rs`'s took `&WebsiteObservation`. Confirmed both types expose identically-signatured `feature_key(&self) -> Option<&str>`, `raw_value(&self) -> &EvidenceValue`, and `normalized_value(&self) -> Option<&EvidenceValue>` accessors, so a new `pub(crate) trait ComparableValue` (with those three methods) was added to `infrastructure.rs`, implemented once for each observation type (each impl trivially delegates to the type's own existing inherent method of the same name), and `values_match` was generalized to `pub(crate) fn values_match<O: ComparableValue>(left: &O, right: &O) -> bool` — unlike the `require_text` case, plain generic calls like `values_match(&left, right)` have no `?`/`From`-conversion involved, so ordinary monomorphization inference resolves `O` from each call site's already-concrete argument types with no ambiguity and no wrapper needed. Net effect: `website.rs` shrank by more lines than `infrastructure.rs` grew (61 removed vs. 34 added), with zero behavioral change anywhere (every moved/generalized body is character-for-character the same logic that ran before, just relocated or accessed through a trait indirection). `cargo build --workspace --locked`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings` (confirming `missing_docs` still does not fire on the new `pub(crate)` trait/impls), `cargo test --workspace --locked` (identical per-file pass counts throughout, including `website`'s 9), and `cargo xtask gates` were all green after the change.
45. **Consolidate three more byte-identical `infrastructure.rs`/`website.rs` helpers (`dependency_group`, `active_weight`, `pair_key`) with the same trait-plus-generic-function pattern established in decisions 42/44, after auditing the rest of the codebase for similarly-shaped duplication and deliberately declining the larger candidates found.** A systematic per-function body hash comparison across all seven engine modules surfaced roughly two dozen same-named, same-body matches beyond what decisions 42/44 already closed. Three were safe to consolidate exactly like `values_match`: the private free function `dependency_group(observation) -> &str` (a four-way metadata fallback chain: explicit field, then source metadata `"dependency_group"`/`"provider"`/`"dataset"`, then the source id) only calls `observation.dependency_group()` and `observation.source()`, both already identically signatured on `InfrastructureObservation`/`WebsiteObservation`, so a new `pub(crate) trait ObservationSource` (those two methods) let it become `pub(crate) fn dependency_group<O: ObservationSource>(...)`. `active_weight(support, mode) -> u16` and `pair_key(support) -> String` operate on the private `Support` struct's `group`/`high_base_rate`/`uncertainty`/`weight`/`left_observation`/`right_observation` fields only (never its `explanation` field, which is the one field whose type actually differs between the two modules' `Support`), so a new `pub(crate) trait SupportFields` (those six accessors) let both become generic; each module keeps its own private `Support` type unchanged (no field-visibility changes, no struct merge) and separately implements the shared trait against its own already-legal same-module field access. Generalizing `active_weight`/`pair_key` first failed to compile at six call sites inside `.max_by`/`.max_by_key`/sort-comparator closures, which receive `&&Support` (double references) rather than `&Support`; the non-generic originals tolerated this via ordinary auto-deref coercion to a concrete parameter type, but a bare generic bound `S: SupportFields` does not auto-deref to find a satisfying `S`. Fixed once, idiomatically, with a blanket `impl<T: SupportFields> SupportFields for &T` (each method delegating via `T::method(self)`, relying on `&&T -> &T` deref coercion) rather than patching every call site — this also protects any future call site from hitting the same issue. Investigated and explicitly declined three larger candidates as lower-confidence/higher-risk than this session's bar: (a) `Support` itself is not merged into one shared generic `Support<X>` type, because doing so would require widening all eight of its fields from module-private to `pub(crate)` across roughly 100 field-access call sites in the two files, for a marginal additional saving beyond what the trait approach already captured; (b) the pervasive nine-factor `calibrated_weight`/`uniform`/`with_uncertainty` quality-scoring trio duplicated across `fusion.rs`/`infrastructure.rs`/`website.rs` is **not** consolidated because every one of those methods is `pub const fn`, and Rust cannot call a generic trait method from a `const fn` on this toolchain — sharing the body would require dropping `const`, a real public-API regression this codebase's pervasive `const fn` idiom clearly values, so the duplication is a deliberate cost of preserving constness, not an oversight; (c) the parallel duplicate-shaped accessor methods on the two top-level engine structs (`observation`, `correlation`, `ranking`, `ranked_correlations`, `supporting_observation_ids`, `with_limits`, `with_maximum_temporal_gap`, engine-config `default`) are not consolidated because each takes or returns other domain-specific `pub` types (`InfrastructureObservation`/`InfrastructureCorrelationReport`/`CorrelationRanking` vs. their `Website*` counterparts); unifying them would cascade into a much larger public-API-reshaping generic-engine refactor, which is deliberately out of scope, consistent with every prior public-API-boundary decision this session. `cargo build --workspace --locked`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked` (identical per-file pass counts, including `website`'s 9), and `cargo xtask gates` were all green after the change.

## Canonical-state and numeric-boundary pass (2026-09-04)

46. **Reconcile every surviving branch before creating new work, and recover only the stronger, non-duplicated part of `c3507e2`.** `main`, the current branch, and the latest successful `gates` run all pointed to `d719744`; no pull request was open. All named historical branches except `copilot/requirements-reconstruction-traceability` were either ancestors of `main` or had trees already represented by the squash-merged main history. Commit `c3507e2` uniquely contained six deterministic property tests for temporal intervals and the advancement/search/fusion/infrastructure/website scoring invariants plus four evidence-store tests. Its empty-identifier test was a strict subset of the current broader constructor test, its documentation correction was already present, and its old tree lacked later work, so a merge would regress the repository. The six property tests and three genuinely unique evidence error-path tests were selectively recovered instead.

47. **Close the gap between observation timeline extension and canonical persistence.** `Observation::seen_at` correctly returned a new immutable observation value with an extended `last_seen`, but `EvidenceStore` exposed only duplicate-rejecting insertion, making the documented repeated-sighting update impossible to persist once an observation was canonical. `EvidenceStore::record_observation_seen_at` now performs a narrow clone-validate-replace transaction: only `last_seen` changes, the existing raw/normalized/source/derivation evidence is retained, and both an unknown ID and time reversal return a typed error before mutation. The focused regression verifies success and both rollback paths.

48. **Require `ble_distance_m` to uphold its positive-finite result invariant across the full finite input domain.** The function validated each input independently but did not validate arithmetic results: opposite signed `f64::MAX` RSSI/reference values overflowed to `Some(inf)` or underflowed to `Some(0.0)`, and a tiny positive path-loss exponent could do the same. The implementation now returns `None` for any non-positive or non-finite computed distance while preserving every representable result. Direct reproductions and a Cartesian sweep over finite numeric boundaries lock the correction in `bleradar-core` 0.4.5.

## Investigation pipeline engine (2026-09-06)

49. **Implement the `DISCOVER → … → STOP ON DIMINISHING INFORMATION GAIN` directive as a new `pipeline.rs` engine, adding only the two capabilities it names that no existing engine provides.** A background-agent mapping of all sixteen stages against the existing engine set found fourteen already substantially implemented (`entity`/`evidence`/`coords`/`osint`/`fusion`/`infrastructure`/`website`), leaving exactly two genuine gaps: a graph that unifies caller-supplied relationship edges with per-node temporal/geographic annotations and reports connected components ("clusters") and cut-edges ("bridges"), and an explicit rule that halts the loop once several consecutive rounds all yield low marginal information gain — `osint.rs`'s `SearchPriorityFactors::expected_information_gain` only weights one candidate pivot against others in the same ranking pass, and its `SearchLimits` only enforce static hard counts, so neither measures actual round-over-round yield. `pipeline.rs` adds `PipelineStage` (a total, cyclic 16-variant enum whose `Display` renders the exact directive wording), `TemporalGeoGraph` (`add_node`/`add_edge` upsert-or-reject-dangling-reference semantics, `clusters()` via iterative DFS, `bridges()` via an iterative — not recursive — Tarjan bridge search, `cluster_temporal_span()` as a deliberately simpler pure-integer min/max type distinct from `infrastructure.rs`'s single-observation `TemporalInterval`), `DiminishingGainStopCriterion` (a sliding window of integer gain samples; stops only once the full window is populated and every sample is at or below threshold), and `InvestigationPipeline` (ties stage tracking to the stop criterion, evaluating it only at the stop-check stage). Cluster geo-summaries deliberately expose only raw per-node positions rather than a synthesized centroid, since `tracking.rs`'s existing antimeridian-safe spherical centroid (fixing bug COR-013) is private to `DeviceTrack` and reimplementing a naive linear-average centroid would risk reintroducing the exact bug class already paid to fix once. The bridge algorithm — the highest-risk new logic, since it is the only genuinely novel graph traversal in this session — was validated with a falsification-first discipline before being trusted: beyond the four textbook test fixtures (simple path: every edge is a bridge; triangle: no bridges; two triangles joined by one edge: only that edge is a bridge; parallel edges between the same two nodes: neither is a bridge), the parent-edge-index-skip logic that makes multigraphs work correctly was deliberately reverted to the classic wrong simplification (skip by parent *node* instead of parent *edge* index) and confirmed the parallel-edges test genuinely fails before restoring the fix; the stop criterion's window-population `all()` check was likewise deliberately swapped for `any()` and confirmed the corresponding test fails before restoring it. Both were momentary, uncommitted edits used only to prove the tests discriminate real bugs, not part of the shipped diff. `cargo clippy --workspace --all-targets --locked -- -D warnings` required two fixes: a named `type Adjacency = Vec<Vec<(usize, usize)>>` alias to resolve a `type_complexity` finding on `indexed_adjacency`'s return type, and collapsing a nested `if let` in `bridges()` into a single `if … && let` chain (stable on this toolchain) to resolve a `collapsible_if` finding. `cargo fmt --all --check`, `cargo test --workspace --locked` (26 new integration tests in `tests/pipeline.rs` plus 4 existing in-module `PipelineStage` unit tests, no regressions elsewhere), and `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps --locked` (after normalizing two doc-links from bare `osint::` to `crate::osint::` for consistency with the rest of the module header) were all verified green, and `docs/REQUIREMENTS_LEDGER.md` gained a `REQ-PIPE` section with one row per verified behavior.

## End-to-end live validation and signal resilience (2026-09-09)

50. **Harden tracking snapshots against adverse spread without losing filtered signal (COR-016, `bleradar-core` 0.5.0).** Live execution of `tracking_snapshot` with NaN/negative/infinite `rssi_spread_db` previously returned `None`, erasing finite filtered RSSI, trend, proximity, and freshness together with bounds/confidence. The failure was reproduced first (`tracking_snapshot_keeps_filtered_signal_when_spread_is_invalid`), then fixed by making distance/bounds/confidence independent soft failures (`confidence_percent: Option<u8>`) while keeping a finite filtered RSSI as the only hard prerequisite. Concurrently, `signal_trend` and `proximity_label` now return `Option` for non-finite inputs so `+inf` cannot read as Immediate and NaN cannot masquerade as Stable/Far; JNI retains conservative ordinal sentinels (Stable/Far/`-1`) for ABI compatibility. API break → minor bump 0.4.5→0.5.0. Re-validated live: `cargo test -p bleradar-core --locked` (core 28, properties 10, full workspace green), `cargo test -p bleradar-jni --locked` (13), `cargo xtask verify-jni-live` (failure-path UnsatisfiedLinkError + success-path abi=6), `cargo xtask verify-android-live` (APK build/sign/align + required entries). REQ-COMPAT-004 remains PARTIAL (no original-oracle on-device differential). Stale REQUIREMENTS_LEDGER deferred bullets claiming REQ-WEB-002 still open were reconciled against the live 13-test website suite. Separately, live `cargo xtask deny` (cargo-deny 0.20.2) failed licenses on `bleradar-jni` because it lacked `publish = false` while `deny.toml` relies on `licenses.private.ignore`; adding `publish = false` restored advisories/bans/licenses/sources ok. Full `cargo xtask gates` re-run after the fixes.
51. **Integrate distance-based proximity without silently changing the legacy RSSI API (2026-09-09).** The oracle contract classifies metres while the established `proximity_label` and `TrackingSnapshot::proximity` classify RSSI dBm. Replacing the existing method would silently change callers, so the smallest complete fix adds `TrackingSnapshot::distance_proximity`, a JNI/Java `trackingDistanceProximity` method, and Android consumption of that additive field while retaining the legacy method unchanged. Boundary tests cover 1/2/5/25 metre classifications, the core test proves the two fields can differ at `-59 dBm`, and invalid JNI inputs conservatively map to `Far`. Acceptance requires the full gate suite plus live JVM→JNI→Rust and APK export verification; the original-oracle on-device differential remains the separate REQ-COMPAT-004 blocker.
52. **Use per-device advertised TX power to calibrate distance instead of only a generic profile constant (REQ-CORE-014, `bleradar-core` 0.6.0, JNI ABI 6→7, 2026-09-09).** `tracking_snapshot` derived every device's distance from a single fixed `CalibrationProfile::rssi_at_1m_dbm`, discarding the per-device calibrated TX power every BLE advertisement can carry (Android `ScanResult.getTxPower()`, available since the app's minSdk 26; `DeviceObservation.tx_power_dbm` existed in `bleradar-core` but was dead — never read anywhere, and never wired to Android). The smallest complete fix adds `signal::effective_rssi_at_1m_dbm(tx_power_dbm: Option<f64>, profile_rssi_at_1m_dbm) -> f64`, which overrides the profile constant only when the device value is finite and within a plausible `[-100.0, 20.0]` dBm range, naturally rejecting Android's `TX_POWER_NOT_PRESENT` sentinel (127) and NaN/Infinity without special-casing; falls back to the profile constant otherwise. Threaded through `TrackingSnapshotInput::tx_power_dbm` (new field, additive API break → minor bump 0.5.0→0.6.0), `TrackingSnapshotJniInput::tx_power_dbm` (NaN sentinel, matching the existing `previous_filtered_dbm` bootstrap convention), all 9 `NativeRadar.tracking*` natives (new trailing `txPowerDbm` parameter → ABI 6→7, `EXPECTED_ABI_VERSION`/`abiVersion()` bumped), and `BleScanEngine` (new `readTxPowerDbm` helper reads `ScanResult.getTxPower()`, stores it on `Blip`, passes it to every `tracking*` call including `refreshFreshness`/`pruneStale`). New tests at every layer: `effective_rssi_at_1m_dbm_prefers_plausible_tx_power`, `tracking_snapshot_prefers_plausible_device_tx_power_over_profile_calibration`, `tracking_snapshot_ignores_implausible_or_absent_device_tx_power` (core, 37 tests total up from 34), `tracking_distance_prefers_plausible_device_tx_power_over_profile_calibration` (jni_bridge, 15 up from 14), plus 2 new live assertions in the `xtask verify-jni-live` harness proving a plausible TX power actually collapses the estimated distance and that the sentinel value is ignored. Full evidence: `cargo test --workspace --locked`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings` (root and `xtask/`), `cargo xtask gates` (fmt/clippy/build/test/doc/parity-report/dependency-policy/oracle-integrity/audit/deny, all green), `cargo xtask verify-jni-live` (failure-path `UnsatisfiedLinkError`, success-path `abi=7`), `cargo xtask verify-android-live` (cross-compiled/signed/aligned APK with `libbleradar_jni.so` verified present). `DeviceTrack`/`DeviceObservation.tx_power_dbm` (a separate, unrelated higher-level API not wired to any JNI/Android code) was deliberately left untouched — out of scope for "the radar," which is the live `tracking_snapshot`→JNI→Android path.
53. **Retroactively document the spatial-estimate/confidence changes bundled into PR #17 (REQ-CORE-010, 2026-09-09).** Copilot's post-merge review of PR #17 (`docs/AUTONOMOUS_DECISIONS.md#L88`, decision #51) correctly flagged that the PR's diff to `tracking.rs` included two behavior changes to `DeviceTrack::spatial_estimate` that decision #51 never mentioned, because decision #51's prose scoped itself only to `distance_proximity`: (a) `observation_weight` gained an exponential recency term (`0.5^(age_ms / 30_000ms)`, floored at 0.05) so a long-lived track's centroid now favors its most recent fixes instead of weighting every historical fix equally by accuracy/signal alone; (b) the reported `uncertainty_m` changed from the arithmetic mean of each fix's `haversine distance to centroid + its own GPS accuracy` to the `max` of that same per-fix quantity, so the displayed radius is now guaranteed to contain every contributing fix's error disk rather than only the "typical" one — closing a real conservatism gap (an outlier fix could previously report itself as safely inside a mean-based radius while actually sitting outside it). A third change, refactoring the confidence-tier if/else chain into the `ACCURACY_CONFIDENCE_TIERS` lookup table, is behavior-preserving (verified below) and needed no separate documentation on its own, but is recorded here for completeness since the reviewer's traceability concern covered the whole diff region. All three were already covered by tests added in the same PR — `spatial_estimate_prefers_recent_position_fixes` (recency dominates: a fix 120s newer collapses the centroid to within 20m of itself while the stale fix is >80m away), `spatial_estimate_uncertainty_contains_every_fix_error_radius` (asserts `uncertainty_m >= max` per-fix error radius, not just the mean), and `map_point_confidence_preserves_accuracy_tiers` (exercises all 6 tier boundaries — 3/5/10/20/50/50.1m — proving the table refactor is behavior-identical to the prior if/else chain) — but the tests were never cross-referenced from `AUTONOMOUS_DECISIONS.md` or `REQUIREMENTS_LEDGER.md`, which is the exact traceability gap the reviewer identified. No code changed in this decision; `REQUIREMENTS_LEDGER.md` REQ-CORE-010 updated to describe recency-weighting and the conservative max-based uncertainty guarantee and cite the three tests above. Re-confirmed live: `cargo test -p bleradar-core --locked --test core` → 37 passed including the three named tests, `cargo test --workspace --locked` green, no behavior change.

## JNI contract, CI reproducibility, and Android design record (2026-09-10)

54. **Make `NativeRadar.java` the single authority for the JNI export contract and enforce it in `gates`, not only on device.** Baseline `origin/main` `b6a8b4a` (CI run 52 green, local `cargo build`/`cargo test --workspace --locked` green) carried the Java↔Rust ABI contract in three hand-maintained copies: the `static native` declarations in `NativeRadar.java`, the `#[unsafe(no_mangle)]` exports in `crates/bleradar-jni/src/lib.rs`, and a 21-entry `REQUIRED_JNI_EXPORTS` list in `xtask/src/main.rs` that only `verify-android-live` consulted — a command the CI runner cannot execute (no Android SDK). A native added or renamed on one side without the other therefore passed every CI gate and surfaced only as an `UnsatisfiedLinkError` at that method's first call on a phone, and the duplicate list could go stale without any check noticing. The smallest complete fix deletes the list and derives the expected set from the Java source itself: `JniExportContract::from_java_source` blanks comments/literals, reads `package`/`class`, collects every `static native` method (rejecting non-static natives and overloads explicitly, since the bridge exports no `__<signature>`-mangled symbols), applies JNI name mangling, and `require_exact_exports` demands the built library's `Java_*` symbols equal that set — missing and orphaned exports are both failures, each named. `cargo xtask check-jni-contract [lib.so]` exposes it (host debug cdylib by default, any ELF64 library by path), `cmd_gates` runs it after `build`, `verify-jni-live` runs it before the JVM, and `verify-android-live` applies it to the cross-compiled `.so`. The JVM harness additionally enumerates the façade's natives by reflection, invokes each with default primitive arguments so the JVM must resolve every export, and fails if its count disagrees with the parser's — so a parser blind spot cannot under-check silently. Evidence: 9 new xtask unit tests (75 total); host library 21 ↔ 21 exact match; the committed APK's `lib/arm64-v8a/libbleradar_jni.so` 21 ↔ 21 exact match; `verify-jni-live` `linked-natives=21`, `abi=7`; falsified by (a) dropping `#[unsafe(no_mangle)]` from `trackingFreshness` → "1 native method(s) … have no export: Java_com_hse_bleradar_NativeRadar_trackingFreshness", (b) adding an orphan export → "1 `Java_` export(s) have no `static native` declaration … _zombie", (c) declaring `zombie(int)` in Java only → missing-export failure, (d) declaring a non-static native → parser rejection, (e) running the harness with the count patched to 20 → JVM failure listing the 21 linked natives; each mutation restored and the exact match re-observed. `gates` is thereby proven on ELF64 hosts (Linux, what CI runs); a non-ELF host library fails with a message naming that limitation rather than silently passing.
55. **Pin and cache the advisory/policy tools in CI, and run the live JVM → JNI → Rust proof remotely on every push/PR.** `.github/workflows/gates.yml` installed unpinned `cargo-audit`/`cargo-deny` from source on every run (5m43s on this host; CI run 52 spent its first five minutes in that step), so a gate result depended on whatever versions crates.io resolved that day even though the advisory *database* they read is vendored and pinned. The workflow now pins `CARGO_AUDIT_VERSION=0.22.2`/`CARGO_DENY_VERSION=0.20.2` (the versions observed green locally), restores `~/.cargo/bin/cargo-{audit,deny}` from `actions/cache@v4` keyed on those versions, the runner OS/arch, and `rust-toolchain.toml`'s hash, builds them only on a cache miss, installs a pinned Temurin 21 JDK (`actions/setup-java@v4`; the local proof ran on OpenJDK 21.0.10), and runs `cargo xtask verify-jni-live` after `cargo xtask gates`. The JVM → JNI → Rust pathway is now remotely regression-locked instead of being proven only in manual sessions. Both actions are first-party GitHub actions, matching the existing `actions/checkout@v4`. Remote evidence is recorded in `docs/REQUIREMENTS_LEDGER.md` (REQ-PROC-006/007) once the first run of this workflow is observed.
56. **Write `docs/ANDROID_APP.md` from tree evidence and record the Android app decisions retroactively.** Six sources (`AndroidManifest.xml`, `colors.xml`, `MainActivity.java`, `NativeRadar.java`, `xtask/src/main.rs`) have cited `docs/ANDROID_APP.md` since PR #13 (`ea842c1`), but `git log --all` shows the file was never committed on any branch, and the decisions behind PRs #13–#15 (no Gradle/AndroidX, `arm64-v8a` only, the permission subset, the palette, the service lifecycle) were never entered in this log — a REQ-PROC-004 gap. The new document records each decision with the evidence it rests on: the oracle's binary-manifest permission set (18 `uses-permission` entries, 5 `uses-feature`) diffed against the rebuilt manifest's 10 kept permissions with a per-row reason; the oracle's single-ABI `lib/arm64-v8a/` layout and library sizes (10,808,624-byte `libbleradar_core.so` vs the rebuilt 318,536-byte `libbleradar_jni.so`); the Gradle-free `build-apk` pipeline; the palette's icon-vector provenance; and the verification matrix with what each proof needs and whether CI runs it. Writing it surfaced two runtime gaps in the reconstructed app that are now open ledger rows rather than hidden behaviour: COR-017 (a native-library load failure is never shown to the user while the UI keeps reporting devices in range) and COR-018 (`START_STICKY` restarts do not resume scanning). Both need a compile-verified Android change, so they are recorded here with their evidence and left open in this decision rather than patched blind.
57. **Fix COR-017/COR-018 and the API 34+ foreground-service ordering hazard (COR-019) with one lifecycle contract, compile-verified and repackaged; leave device observation explicitly open.** Reading `MainActivity`/`RadarScanService` for decision #56 found a third defect on the same path: `onStart` called `startForegroundService` on every activity start and `onStartCommand` immediately promoted with `FOREGROUND_SERVICE_TYPE_CONNECTED_DEVICE`, before any runtime permission had been granted on a fresh install. With `targetSdkVersion=34`, Android 14+ requires one of the `BLUETOOTH_SCAN`/`BLUETOOTH_CONNECT`/`BLUETOOTH_ADVERTISE` runtime permissions (or a normal permission this manifest does not declare) to be held when a `connectedDevice` service is promoted and throws `SecurityException` otherwise, so the first launch on a current device would crash the service before the user could grant anything — a break of INSTALL → START → USE that no prior "live" proof could see, because none ran on a device (MIG-003). The three defects share one root cause: promotion and scanning were tied to activity visibility instead of to a scan request. The new contract ties both to the request: the activity only binds while visible; the Start action checks permissions and the adapter, then issues `startForegroundService` and `startScanning()`; `onStartCommand` — reached only by that explicit start or by the `START_STICKY` restart after process death while scanning — re-checks the permissions, starts/resumes the scan, and promotes with the scanning notification (leaving the foreground and stopping itself if the scan cannot start, or stopping with `START_NOT_STICKY` if a restart finds the permissions revoked); Stop calls `stopForeground(STOP_FOREGROUND_REMOVE)` and `stopSelf()` so a deliberate stop is never resurrected. This closes COR-018 without any persisted flag (the started state *is* the record of the request), keeps no idle notification while the app is idle, and makes `refreshUiLoop` return the button/status to idle when a scan ends without a toggle. COR-017 is closed by routing every status line through `MainActivity.setStatus`, which appends `status_native_unavailable` (naming the load-error class) whenever `NativeRadar.isAvailable()` is false. To make the change verifiable at all, the Android SDK (command-line tools `11076708`, build-tools 37.0.0, platform android-36) and NDK 27.3.13750724 were installed into this sandbox, so `cargo xtask verify-android-live` ran here: `javac` against `android-36` compiled the new sources, `d8` packaged them, `dexdump` of the rebuilt DEX shows `onStartCommand`/`promoteToForeground`/`stopScanning`/`setStatus`, `stopForeground(int)`, and `startForegroundService`, `resources.arsc` carries the new string, the cross-compiled `libbleradar_jni.so` is byte-identical to the committed one, and the export contract matched 21 ↔ 21. The committed `HSE-BLE-Radar-arm64-v1.0.0.apk` was regenerated from these sources. Before that regeneration, rebuilding the then-committed sources showed the previous APK was source-current (identical class/method inventory and instruction stream) and differed only by JDK-version metadata (JDK 21 `MethodParameters`), recorded in `docs/COLD_START_VERIFICATION.md`/`docs/ANDROID_APP.md` so a future DEX diff can be classified in seconds. What this decision does not claim: first-launch, process-kill, permission-revocation, and no-native behaviour on a device or emulator remain unobserved, so REQ-ANDROID-002/003 are `IMPLEMENTED_UNVERIFIED` with those exact missing observations named, and COR-017/018/019 are marked remediated at the compile/package level only.

## Empirical falsification and measured baselines (2026-09-10, later)

58. **Run a deterministic randomised falsification campaign over the real crate, fix what it finds, and keep the campaign as a permanent regression test.** With on-device evidence still blocked (`/dev/kvm` absent and no `vmx`/`svm` CPU flags on this host, so the Android emulator cannot run; recorded rather than attempted), the remaining empirically testable surface was the Rust core itself. A dependency-free harness (xorshift64* PRNG, fixed seed, random Unicode/byte strings plus mutations of valid seeds, homoglyph substitution, random `f64` bit patterns including NaN/inf/subnormals) exercised `coords::parse`, `entity::normalise`/`uid_for`/`derive_uid`/`canonical_handle`, `HseEntity` construction/decay/merge, `canonical_mac`/`is_locally_administered`, `tracking_snapshot` and the signal helpers, `DeviceTrack`/`spatial_estimate`, and `haversine_m`/`bearing_deg`, under oracles for panics, finiteness/range, EMA bounds, bracketing bounds, and idempotence. 5,000,000 iterations (36.25 million checks, 51 s) produced four reproducible defects, each locked by a regression test that failed on the unfixed code before the fix was applied: COR-020 (`olc_index` truncated non-ASCII characters to a byte, so `"4RRH46RW+RH😷"` decoded as a valid Plus Code), COR-021 (the `Coordinates` fast path skipped the `LatLon::new` range gate and canonicalised `"100,2000"`, and turned `"1e308,0"` into `"inf,0.000000"`), COR-022 (`normalise(Domain)` was not a fixed point when whitespace followed a peeled `www.`), and COR-023 (`normalise(Url)` was not a fixed point for whitespace before a trailing slash or at the end of the last query parameter). All four are invisible for valid inputs — they change only the handling of forged, impossible, or whitespace-corrupted values — so UIDs of valid entities are unchanged; the deviations from the upstream HSE behaviour are recorded in `docs/HSE_IMPORT.md`. After the fixes, three seeds × 5,000,000 iterations (108.75 million checks) report zero violations; the two other report classes the reruns produced were harness-oracle artefacts (printing subnormals in full decimal exceeds the parser's 256-character cap; Plus Code cell centres round to 6 dp at exactly the old tolerance) and were corrected in the harness, not the crate. `bleradar-core` is bumped 0.6.0→0.6.1 (bug fixes, no API change). The cross-compiled `libbleradar_jni.so` changed with the core (318,536→318,504 bytes; only that APK entry differs, the DEX and resources are byte-identical), so the committed APK is regenerated and `verify-android-live` re-run green. The campaign is retained in-tree as `crates/bleradar-core/tests/falsification_campaign.rs`: 10,000 iterations by default so every `cargo test` run keeps probing, scalable with `BLERADAR_CAMPAIGN_ITERATIONS` and reseedable with `BLERADAR_CAMPAIGN_SEED`, so a future finding is reproducible from its seed.
59. **Establish a reproducible host baseline for the per-scan-result JNI cost and reject the single-call consolidation candidate on the measurement.** `BleScanEngine.recordResult` calls eight `NativeRadar.tracking*` natives that each recompute the same `tracking_snapshot`; consolidating them into one packed call had been a standing efficiency candidate. `crates/bleradar-jni/examples/scan_result_cost.rs` (dependency-free, deterministic inputs, best of five rounds of 10,000,000 iterations, compiled by every `clippy --all-targets` gate) measured 129.2 ns per `tracking_snapshot`, 1,075.5 ns per scan result for the eight-call pattern, and 8.6 ns for the bare EMA on this host (rustc 1.98.0, 4 vCPUs). Adding roughly 0.1 µs per JVM→native transition, one scan result costs on the order of 2 µs of native work — about 0.02 % of one core at 100 scan results per second — so the consolidation would add ABI surface and a Java-side decoder for no observable gain. The candidate is rejected on that evidence and the numbers are recorded in `benchmarks/README.md` as the baseline a future change must move before the decision is revisited.
60. **Exercise the offline entity, evidence, OSINT-frontier, and pipeline engines with the operator's own identifiers as real inputs, locally, and record what that showed.** At the operator's request, a scratch program (not committed: the packaging rules exclude personal content, and none of the values are reproduced here) fed the operator's email, GitHub handle, display name, mail domain, and two repository URLs through `normalise`/`uid_for`, `HseEntity` construction/merge, `ExecutionFeedbackAdaptiveOsintSearchEngine` (seeds under `Exact`/`Semantic`, generated pivots under `Alias`/`Relational`/`Technical`, a deterministic local executor standing in for the network adapter the workspace deliberately does not contain — nothing left the process), and `InvestigationPipeline`/`TemporalGeoGraph`. Observed: padded and case-variant spellings of the email, handle, domain, and person reach one UID each; merging a case variant of the email yields three distinct sources, `c_effective` 0.831, class `VERIFIED`; the frontier ran 8 executions over 8 pivots with all 7 phases recorded per execution, useful `Exact` results raised that family's pressure so the second seed ran next, a duplicate `Technical` outcome demoted the remaining `Technical` pivot below an untried `Semantic` seed, and the loop terminated with no ready pivot; the six identifier nodes formed one cluster whose five tree edges were all reported as bridges; the pipeline logged the 16 stages in order and stopped after the fourth round, the first time two consecutive rounds had zero gain (window 2, minimum 0), and refused `advance()` afterwards. No invariant was violated and no defect was found. Two behaviours are recorded as design observations rather than defects: a generated pivot whose query equals an existing pivot's under a different `SearchRepresentation` is executed again (representations are distinct search strategies, so this is intended), and `normalise(Url)` keeps path case per RFC 3986 even for hosts that treat paths case-insensitively (site-specific canonicalisation is a policy above this crate). External OSINT lookups against third-party services are outside the codebase (no network code by design) and were not performed.
61. **Lock the multigraph-safe bridge/cluster claim with a brute-force differential oracle.** README and REQ-PIPE-005 claim an "iterative, multigraph-safe Tarjan bridge search", but every existing test was a hand-built case. The claim has a cheap independent oracle — an edge is a bridge iff removing it increases the number of connected components, computable with union-find — so a scratch harness compared `TemporalGeoGraph::{clusters, bridges}` with that reference on 50,000 random multigraphs (1–9 nodes, up to 14 edges, self-loops, forced parallel edges): zero mismatches. The oracle is retained as `clusters_and_bridges_match_a_brute_force_reference_on_random_multigraphs` in `crates/bleradar-core/tests/pipeline.rs` over a deterministic 2,000-graph sample (0.11 s), so a future refactor of the bridge search is checked against the definition, not against remembered cases. No production code changed.
62. **Run a random-operation campaign over the canonical `EvidenceStore`, confirm its insertion/validation split, and keep the campaign as a test.** The store is the authoritative provenance record every other engine writes to, so its two guarantees — insertion enforces referential integrity and a rejected operation never mutates the store; `validate()` re-checks every cross-reference and provenance invariant — were exercised with random sequences of all sixteen operations (`insert_source` … `insert_confidence_update`, `record_observation_seen_at`, `trace_claim`, `observations_by_source`) over a five-id-per-type space so references collide, dangle, arrive out of order, and break the temporal and verification rules. Oracles: no panic; a rejected operation leaves `len()` unchanged; `validate()` is green after every operation except for the documented transient `ClaimWithoutEvidence` incompleteness, which must agree with `trace_claim`; `trace_claim` and `observations_by_source` agree with a manual recount. 20,000 sequences × 250 operations (5,000,000 operations: 1,686,636 accepted; 3,313,364 rejected as 1,137,289 `DuplicateId`, 2,070,971 `MissingReference`, 48,274 `SourceMetadataMismatch`, 44,165 `InvalidTimeline`, 12,665 `TemporalViolation`) produced zero violations. The first harness draft reported two classes of "violation" that were harness mistakes, not store defects, and are recorded so they are not rediscovered: `validate()` fails with `ClaimWithoutEvidence` for a claim inserted before its evidence, which `claim_without_any_evidence_is_rejected_by_trace_and_validate` shows is the intended split (order-independent insertion, completeness enforced by `validate()`/`trace_claim`); and a constructor refusal (`FeatureInBothSets`, an empty verification test list, an inverted timeline) is a typed refusal before any store operation, not a mutation. The campaign is retained as `crates/bleradar-core/tests/evidence_campaign.rs` (100 sequences per `cargo test`, scalable via `BLERADAR_EVIDENCE_CAMPAIGN_SEQUENCES`, reseedable via `BLERADAR_EVIDENCE_CAMPAIGN_SEED`). No production code changed.
63. **Run a random-operation campaign over the OSINT search engine and keep it as a test.** `ExecutionFeedbackAdaptiveOsintSearchEngine` is the engine an operator's own identifiers flow through (decision #60), and its contract is largely about what it refuses and what it leaves untouched: limits, single execution per pivot, transactional persistence into the canonical store, duplicate-seed suppression, and a ranking that only ever offers `Proposed` pivots. A harness drove random `add_pivot`/`execute_result`/`exhaust`/`recompute_rankings` sequences under random limits (1–8 executions, 1–12 pivots) with feedback carrying conflicting sources (same id, different `SourceType`), conflicting findings (same id, different value), duplicate seeds across representations, adapter failures including an empty error message, and invalid factors (zero cost/risk). Oracles after every operation: no panic; `execution_count() ≤ max_executions` and `pivot_count() ≤ max_pivots`; an executed pivot is marked `Executed` and never executes again; a refused operation leaves execution/pivot counts, every known pivot's state, and the evidence store's length unchanged; each generated pivot id is fresh, `Proposed`, and carries its parent; findings and the retrieval action are persisted; `ranked_pivots()` is unique and `Proposed`-only, `next_pivot()` is its head and `has_ready_pivot()` agrees; family statistics sum to the execution count; the engine's evidence store passes `validate()`. 50,000 sequences × 60 operations (3,000,000 operations; 151,325 successful executions; refusals: 997,791 `InvalidState`, 638,633 `ResourceLimit`, 292,089 `MissingPivot`, 132,418 `DuplicatePivot`, 64,231 `SourceConflict`, 25,664 `EmptyValue`, 24,230 `FindingConflict`, 10,213 `DuplicateQuery`) produced zero violations. Retained as `crates/bleradar-core/tests/osint_campaign.rs` (100 sequences per `cargo test`, `BLERADAR_OSINT_CAMPAIGN_SEQUENCES`/`BLERADAR_OSINT_CAMPAIGN_SEED`). One design note for future readers, not a defect: `record_feedback` clones the whole evidence store per execution to stay transactional, so a long investigation's cost grows with the store; the default limit of 128 executions keeps that immaterial today. No production code changed.

64. **Run a random-operation campaign over the website lineage and infrastructure correlation engines, fix the three defects it found at their shared root, and keep both campaigns as tests.** `WebsiteLineageEcosystemAnalysisEngine` and `TemporalMetamorphicInfrastructureCorrelationEngine` are the remaining engines an operator's own identifiers flow through (decision #60); their contract is transactional persistence into the canonical store, enforced limits, and a persisted, falsified, score-ordered ranking. A harness drove random `observe`/`observe_snapshot`/bulk-observe/`correlate`/`correlate_all`/`ranked_correlations` sequences under random limits (1–80 observations, 1–12 correlations, continuity window 0/5 ms/1 day) with colliding ids, conflicting sources, a pre-seeded conflicting canonical observation, empty and unknown identifiers, and bursts of up to 40 observations from distinct sources so one correlation carries up to 1,600 independent supports. Within the first sequences it found three defects, all reproduced, root-caused, and fixed with regression tests that fail on the unfixed source (verified by stashing only the two source files: 4 of 17 infrastructure and 3 of 16 website tests fail, then pass again): **COR-024** — both engines' per-explanation ranking summed temporal compatibility in a `u16`, so ≥ 656 overlapping independent supports (26 observations per side, inside the default limit of 10,000) panicked in debug builds and, in the release profile the Android library ships with, silently reported a mean of 3 instead of 100; the corroboration term overflowed at 6,555 supports and the truncated divisor reached zero at 65,536. Fixed once, for both engines, with shared `corroborated_score`/`mean_temporal_compatibility` helpers in `infrastructure.rs` (usize/u64 arithmetic, unit-tested at 656 and 65,536 supports), and the same function's quadratic high-base-rate scan replaced by a precomputed pair set with identical semantics: a 625-pair correlation now takes 9.7 ms instead of 110 ms (website) and 5.8 ms instead of 40 ms (infrastructure), 10,000 pairs 220 ms / 121 ms. **COR-025** — `correlate_all` aborted with `DuplicateCorrelation` after any earlier manual `correlate` and left the pairs it had already persisted in place (the same partial state on a mid-batch `ResourceLimit`); it now runs on a clone swapped in only on success, skips pairs already correlated in either direction, and returns only what it persisted, so it is all-or-nothing and idempotent like `observe_snapshot`. **COR-026** — the infrastructure edge and its persisted relationship provenance cited an observation once per retained pair (1,352 ids for 52 observations) where the website engine cited each once; one shared `unique_observation_ids` now serves both. With the fixes in place, 20,000 sequences × 60 operations per engine (1,200,000 operations each, bulk observations counted singly: website 830,377 accepted / 716,727 refused as 322,882 `ResourceLimit`, 125,937 `EmptyValue`, 84,479 `SourceConflict`, 71,699 `MissingWebsite`, 51,083 `SameWebsite`, 29,742 `DuplicateObservation`, 14,782 `NoComparableObservations`, 13,810 `DuplicateCorrelation`, 2,313 `ObservationConflict`; infrastructure 912,217 accepted / 779,475 refused as 452,316 `ResourceLimit`, 126,274 `EmptyValue`, 73,919 `MissingNode`, 51,054 `SameNode`, 33,377 `SourceConflict`, 13,862 `NoComparableObservations`, 13,714 `DuplicateObservation`, 13,118 `DuplicateCorrelation`, 1,841 `ObservationConflict`) produced zero violations. Retained as `crates/bleradar-core/tests/website_campaign.rs` and `tests/infrastructure_campaign.rs` (100 sequences per `cargo test`, about 2 s each in debug, each asserting it reached a correlation with more than 655 independent supports so the overflow regime is exercised on every run; `BLERADAR_WEBSITE_CAMPAIGN_SEQUENCES`/`_SEED`, `BLERADAR_INFRASTRUCTURE_CAMPAIGN_SEQUENCES`/`_SEED`). `bleradar-core` 0.6.1 → 0.6.2. The APK was rebuilt from the new sources and every entry, the native library included, was byte-identical to the committed one (the JNI library does not link these engines), so the committed artifact is unchanged. One semantic note: `correlate` itself still accepts a second, reverse-direction correlation of a pair on explicit request; only the batch treats the unordered pair as covered.

65. **Call the 21 exported JNI symbols themselves, with null `JNIEnv`/`jclass`, against their pure cores, the underlying `bleradar-core` functions, and the UI's domain invariants over adversarial inputs, on every `cargo test`.** The exports are the production path the Android app exercises on every scan result, and the release profile they ship with aborts the process on any panic; yet `tests/jni_bridge.rs` only called the pure cores, so a wrapper that swapped two arguments or drifted a sentinel was caught only by the JVM harness's handful of fixed values. Every export ignores its environment and class arguments (module docs), so calling the exported `extern "system"` functions from Rust with `null_mut()` is ordinary safe code. `crates/bleradar-jni/tests/jni_campaign.rs` (`every_export_agrees_with_its_core_and_never_panics`) draws RSSI/spread/alpha/exponent/TX-power values from a mixture of plausible magnitudes, a special-value list (NaN, ±∞, ±0, subnormals, `f64::MAX`/`MIN`, 127 — Android's `TX_POWER_NOT_PRESENT`), and arbitrary 64-bit patterns, plus `i32`/`i64` extremes, invalid ordinals, negative counts and ages, and checks per iteration: no panic; export == pure core == `bleradar-core` bit-for-bit (NaN with NaN) for all 21 symbols; ordinals within their documented ranges; distances NaN or finite-positive; bounds NaN together, ordered, and enclosing the central estimate; confidence `-1` or `0..=100`; a stronger signal never reads farther (`bleDistanceM`, `proximityLabel`); `signalTrend` antisymmetric in its two samples; confidence monotone in support and spread; and, for the eight `tracking*` exports, that a rejected input (negative count or age, unknown ordinal, non-finite sample) yields every invalid sentinel and nothing else does, that an accepted input's fields agree with `filtered_rssi`, `signal_trend` under the profile deadband, `proximity_label`, `proximity_label_from_distance_m`, `signal_confidence_percent`, and `FreshnessClass::from_age` under the profile windows, and that a bootstrap (non-finite previous) adopts the current sample with a `Stable` trend. 20,000 iterations per `cargo test` (0.12 s); 2,000,000 iterations in release produced zero violations. Falsified by two deliberate mutations, each restored afterwards: swapping `bleDistanceM`'s first two arguments in the export failed at iteration 0, and mapping a non-finite RSSI to `Mid` instead of `Far` in `proximity_label_ordinal` failed at iteration 28, both naming the export and the inputs. Scalable via `BLERADAR_JNI_CAMPAIGN_ITERATIONS`, reseedable via `BLERADAR_JNI_CAMPAIGN_SEED`. No production code changed; the crate-wide sweep for the COR-024 arithmetic pattern (`u16`/`u8` sums and `len() as u16` divisors) found no other instance.

66. **Give the canonical store one journaled `transaction` primitive and make every engine use it instead of cloning the store per operation.** Measured on the reproducible `examples/engine_load.rs` (release, host): loading 8,000 website observations took 21.0 s, 2.6 ms per `observe` and growing linearly with the store (234 µs at 1,000 records), because `observe`, `correlate`, `observe_snapshot`, `correlate_all` (website and infrastructure) and `record_feedback` (OSINT) each cloned the whole `EvidenceStore` — or the whole engine — to obtain all-or-nothing semantics; the documented 10,000-observation capacity was reachable only at tens of seconds per load, and the OSINT design note in decision #63 was the same cost. Root cause: atomicity was implemented seven times at the callers, each by copying the authority, instead of once at the authority. `EvidenceStore::transaction(|store| …)` now runs a closure and, on `Err` (or a panic, via a drop guard), undoes every change it made in reverse order from an undo journal that is only kept while a transaction is open: every `insert_*` journals a `Remove(kind, id)`, `record_observation_seen_at` journals the replaced observation, and `insert_representation` journals the artifact whose representation list it extends — the three mutation shapes the store has. Transactions nest (an inner failure undoes only the inner changes); the error type is the caller's; a crate-internal `transaction_through` lets an engine run a transaction through a method that needs the whole engine (`correlate_all`). The engines were converted to check-then-transact: reads (limits, duplicates, source and canonical-record conflicts) against the live store, then one transaction for the writes, then the engine's own indexes; batch operations wrap their items in an outer transaction and drop their own index entries on failure; the OSINT feedback path validates findings, the retrieval action, and the generated pivots inside one transaction in the original order, so no engine error precedence changed. After: 3.2 µs per website `observe` at 8,000 records (25.8 ms for the load, ~800× faster, flat from 1,000 to 8,000), 3.5 µs per infrastructure `observe`, 2.5 µs per OSINT execution (was 68 µs). Proven by: four new store tests (`transaction_keeps_every_change_on_success_and_undoes_every_change_on_error`, `nested_transaction_failure_undoes_only_the_inner_changes`, `transaction_rollback_restores_in_place_updates`, `panic_inside_a_transaction_rolls_back_and_leaves_the_store_usable`), every existing engine transactionality test, and the evidence/OSINT/website/infrastructure campaigns, all green; falsified by disabling the undo of inserts, which failed the four store tests, the engine rollback tests (`correlate_all_leaves_the_engine_unchanged_when_the_correlation_limit_is_hit` in both engines, `snapshot_conflict_on_the_last_observation_still_rolls_back_earlier_successes`), and all three engine campaigns, then restored. `bleradar-core` 0.6.2 → 0.6.3 (one new public method). The example is compiled by every `cargo clippy --all-targets` gate; its numbers are recorded in `benchmarks/README.md`. The APK rebuilt from these sources differs from the committed one only in 151 bytes of the native library's dynamic symbol/string tables, relocations and ELF headers (identical instruction stream, section sizes, exports and imports — see `docs/COLD_START_VERIFICATION.md`); it was regenerated and committed so the artifact stays reproducible from the integrated commit. After the change, the decision #64 scratch harness was re-run against the transactional engines at 20,000 sequences × 60 operations per engine (website 828,235 accepted / 716,669 refused; infrastructure 928,056 accepted / 779,168 refused; every refusal class represented) with zero violations. The evidence-store campaign (`tests/evidence_campaign.rs`) now also draws random transactions — nested up to two levels, holding one to four random operations, aborted by error or by panic half of the time — and requires the store's complete state (its `Debug` rendering, journal included) to be exactly restored when a transaction does not commit and nothing to be lost when it does; 5,000 sequences × 250 operations in release produced zero violations, and disabling the undo fails it at the first aborted transaction.

67. **Compare the fusion engine against an independent reference scorer on random stores, on every `cargo test`.** `CalibratedEvidenceFusion` is the last engine in the OSINT analysis chain (decision #60) without a campaign; it is pure over a store, so the right oracle is differential: `crates/bleradar-core/tests/fusion_campaign.rs` builds random valid stores (1–4 sources, 1–3 competing hypotheses, 1–8 observations, up to 12 supporting/contradicting/contextual evidence items), random assessments (uniform and nine-dimension qualities, explicit `g0`/`g1` and blank groups, every `DependencyKind`, high-base-rate flags, uncertainty, unregistered evidence ids) and random expected-evidence items (present, missing, unregistered hypotheses), then checks `fuse()`, `fuse_hypotheses()` over a subset with an unregistered id, and `falsify()` against a reference written from the module's documented rules: one contribution per independence group with the first strongest member retained and the rest reported as collapsed, weights from `calibrated_weight`, zero weight for high-base-rate items in the removal pass and `base × (100 − uncertainty) / 100` in the perturbation pass, net = support − contradiction, ordering by net score then hypothesis id, refusals exactly `MissingEvidence` / `MissingHypothesis` / `NoHypotheses`, and the report's strongest alternative, strongest support group (highest first-strongest per-group weight, ties to the smallest group name), removed support, missing expected evidence, contradictory evidence and survival verdict. 2,000 iterations per `cargo test` (0.17 s); 300,000 iterations in release produced zero violations. Falsified by three deliberate mutations, each restored: letting a later equal-weight group member replace the retained one (caught at iteration 800), subtracting only half of the contradiction score (iteration 4), and tolerating one missing expected item in the survival verdict (iteration 7). `MetamorphicTest::new` was also checked for the relation-pair indexing in `verification.rs` and validates the input count per relation, so no campaign is needed for that path. No production code changed.

68. **Run a random-operation campaign over the verification engine and keep it as a test.** `VerificationEngine` is what the advancement engine's accept/reject decisions rest on, and its guarantees are procedural: every non-retired test executes, a violation is minimized without changing what it proves, family pressure accumulates and decays exactly, and a differential run classifies each case from the contract's surfaces. `crates/bleradar-core/tests/verification_campaign.rs` drives random required semantics (explicit surface subsets or every observable surface; one to three relations), random metamorphic tests over all nine relations (minimum and larger input counts, random bytes with a 0xFF poison byte), deterministic adversarial executors chosen per sequence (six output transforms, failure on the poison byte, states, side effects, exit codes, orderings, optional input representation and performance measurement, monotonic values), retire/reinstate (retire-biased), regression-lock and repair transitions, `verify`, and `differential` over random cases. Oracles: no panic; a `RelationNotRequired` refusal names the first non-retired disallowed test and leaves family pressure untouched; executed/inconclusive/passed/retired/lock/repair accounting equals an independent recount and `passed()` equals its definition; every violation names an executed test with in-range distinct indices, a cause consistent with its differing surfaces, differing surfaces equal to a reference comparison written from the surface definitions, minimized inputs that are subsequences of the originals and untouched outside the violating pair, and — re-verified from scratch on a fresh engine — the same pair still fails with the same cause and surfaces; differential reports partition their cases, classify inconclusiveness and differing surfaces exactly as the reference, and their minimized inputs still differ the same way; family statistics (executions, violations, saturating pressure growth and unit decay, retired counts), `family_pressure` and `prioritized_relations` match the reference after every run. The campaign asserts it exercised accepted and refused runs, violations and at least one pressure decay — the first draft never reached the decay branch (a retired violating test followed by a clean run) and a mutation removing the decay went uncaught until retirement was made more likely and the coverage assertion added. 150 sequences × 24 operations per `cargo test` (0.12 s); 20,000 sequences in release produced zero violations. Falsified by three mutations, each restored: no pressure decay (caught at sequence 22), minimization accepting a candidate that changes the surfaces (sequence 1), inconclusive tests counted as passed (sequence 10). A crate-wide sweep of panic paths in library code found ten `expect` calls, each behind a validated precondition, and no `unwrap`/`panic!`/`unreachable!` outside doc examples; `Confidence::new` clamps to 100, so the uncertainty subtractions in the engines cannot underflow. No production code changed.

69. **Find comparable observation pairs through value indexes instead of a left × right scan; borrow observations and compare pair keys without allocating.** With `observe` flat (decision #66), `examples/engine_load.rs` showed one `correlate` over two websites holding 4,000 observations each taking 730 ms (infrastructure 500 ms): `comparable_pairs` tested every left observation against every right one with `values_match` — 16,000,000 comparisons for 16,000 matches — and cloned both observations into every pair. A first attempt that only replaced the clones with references made the 8,000-record case slower (945 ms), because the clones had given the scan contiguous memory; the measurement, not the intuition, located the cost in the scan itself. `matching_pairs` (shared, `infrastructure.rs`) now indexes the right-hand observations by feature key, normalized value and raw value (through an orderable `ValueKey` projection of `EvidenceValue` in which signed zeros share a key and NaN has none) and proposes candidates for each left observation from exactly the clauses of `values_match`, re-checking each candidate with `values_match` itself, so the pair set is provably the scan's; a 300-round unit test compares it with the scan on random observations over every value variant. `ComparablePair` borrows its observations, and tie-breaking between supports compares the `"{left}:{right}"` pair keys slice-wise without building them (`pair_key_cmp`, byte-identical ordering to the former concatenated strings, including identifiers that contain the separator). Measured (release, host): website `correlate` at 16,000 pairs 730 ms → 470 ms, infrastructure 500 ms → 245 ms, both now dominated by ranking; every website/infrastructure test and campaign is unchanged and green. No observable behaviour changed.

70. **Compare the advancement engine's decision, state machine and ranking against an independent recomputation, on every `cargo test`.** `MetamorphicSoftwareAdvancementEngine` is the last engine without a campaign and the one whose whole job is combination: it accepts a proposal only if eight gates all pass — baseline and candidate semantics preserved, differential agreement, verification coverage, measurable improvement, no unexplained material regression, falsification resistance, reproducibility — and it drives a `Proposed → Accepted/Rejected → Integrated` state machine and a priority ranking that excludes settled proposals. `crates/bleradar-core/tests/advancement_campaign.rs` drives random `add_proposal`/`evaluate`/`integrate` sequences with random factors, random executions (0–3 differential cases, 0–3 benchmark metrics with random directions/thresholds/explanations, a resistant or randomly-built falsification result, a reproducibility flag) and correct or deliberately-broken baseline/candidate executors against a fixed normalization contract. The oracle recomputes each gate's input independently — the two verification reports and the differential report from a standalone `VerificationEngine` built identically (verify/differential are themselves campaign-covered, decision #68), the benchmark/falsification/reproducibility gates from the execution — and assembles the expected rejection reasons in the engine's own source order, so `decision.rejection_reasons()` is compared for exact equality (set and ordering), `disposition()`/`accepted()` against whether that list is empty, the engine's stored state against the decision, `evaluate` on an unknown or already-settled proposal against `MissingProposal`/`InvalidState`, `integrate` against the `Accepted`-only gate, and `ranked_proposals()`/`recompute_rankings()` against a reference filter-and-sort after every operation. The generators are asserted to reach acceptance, rejection and integration. 400 sequences × 20 operations per `cargo test` (0.17 s); 50,000 sequences in release produced zero violations. Falsified by three mutations, each restored: accepting a proposal with a single rejection reason (caught at sequence 2), dropping the zero-differential-cases arm of the coverage gate (sequence 1), and allowing `integrate` from a non-`Accepted` state (sequence 0). The first draft carried a `retire_all` dimension that retired the oracle's test but not the engine's fixed verification engine; the mismatch it produced was a harness bug, corrected by modelling the engine's never-retired verification engine, not a crate defect. This closes the campaign frontier: every engine that persists to the store, scores, ranks, or gates — evidence, OSINT, website, infrastructure, fusion, verification, advancement — plus the pipeline's bridge/cluster oracle and the 21 JNI exports now has a falsification or differential campaign retained as a test. No production code changed.

71. **Make the documented "one canonical store" composition reachable by moving the store between engines instead of cloning it.** The README states that "other engines should write to this store rather than maintaining parallel evidence histories", but every store-owning engine (`ExecutionFeedbackAdaptiveOsintSearchEngine`, `WebsiteLineageEcosystemAnalysisEngine`, `TemporalMetamorphicInfrastructureCorrelationEngine`) took `EvidenceStore` by value and exposed only `evidence(&self)`/`evidence_mut(&mut self)`. So a caller threading one investigation through several engines could not pass the canonical store from one engine to the next: the only options were `evidence().clone()` at each hand-off — the O(store) copy decision #66 removed *inside* engines, still forced *between* them — or the parallel histories the design forbids. Reproduced on a scratch composition (OSINT self-lookup → infrastructure correlation → website lineage → fusion, all over one store): the composed store validates and carries both cross-engine relationship edges, but the three hand-off clones cost 6.9 + 3.0 + 2.4 ms at 16,000 records and grow linearly. The fix is an additive `into_evidence(self) -> EvidenceStore` on the three engines, so the caller moves the store into the next stage at zero copy. Measured with `examples/engine_load.rs` (release, host): `evidence().clone()` 97 µs → 997 µs from 1,000 to 8,000 records; `into_evidence()` ~400 ns, flat, at every size. Proven by `crates/bleradar-core/tests/composition.rs`, which threads one store through all four engines with `into_evidence`, validates the composed store, and asserts every engine's records and both relationship edges are present, plus a second test that the move-threaded and clone-threaded composed stores are byte-identical (`Debug` equality) at three store sizes — so `into_evidence` changes only the cost, not the result. Falsified by making one engine's `into_evidence` return an empty store, which failed both composition tests, then restored. A third composition test pins a robustness property the per-engine campaigns never exercised: when a later engine is handed a store where an earlier engine already wrote an observation id, its colliding `observe` is refused with `ObservationConflict`, the shared store is left byte-identical (the original observation intact, no dangling entity for the refused record) and still valid — verified empirically (OSINT writes `finding`, infrastructure's colliding certificate observation is refused and its `node` entity never created) and falsified by inverting the infrastructure conflict check, which failed only that test. This is the first end-to-end exercise of the composed multi-engine runtime path; the per-engine campaigns each used a fresh store, so nothing had previously proven the engines compose over one canonical store. `bleradar-core` 0.6.3 → 0.6.4 (three additive methods). The JNI library links none of these engines, so the cross-compiled `.so` is unaffected and the committed APK is unchanged.

72. **Execute the immutable native oracle under `qemu-aarch64` against a real Bionic runtime and promote the WiFi channel↔frequency contracts from `SourceAnalog` to `DifferentiallyVerified`.** The whole project exists to prove the safe-Rust reconstruction equivalent to the shipped native core, yet every pure contract was only `SourceAnalog`: the source was compared against a *hand-reconstructed model* of the oracle (`oracle_characterization.rs::oracle_wifi_*`), never against the oracle binary, because it is an `aarch64` Android library and the sandbox has no device or emulator (MIG-001/002/003: `/dev/kvm` absent, no ARM64/Bionic host). That blocker was re-tested rather than assumed and found surmountable: the oracle is a UniFFI library whose scaffolding exports (`uniffi_bleradar_core_fn_func_*`) are plain C-ABI functions needing no JVM, and it links only `libc`/`libm`/`libdl`, so it runs under `qemu-aarch64` user-mode emulation once given a real Bionic runtime. `qemu-user-static` installs from apt; `linker64` + `lib{c,m,dl,c++}.so` extract from an `arm64-v8a` system image with `debugfs` (no root, no loopback); the NDK cross-compiles a harness that links the oracle and calls it. A known-answer test validated the whole pipeline: the executed oracle returns `haversine_m(0,0,1,0) = 111195.0802335329` bit-for-bit, reproducing the recorded `ORACLE_EQUATORIAL_DEGREE_M` fixture. The function signatures were decoded from the oracle's `UNIFFI_META_*` metadata blobs (type codes `06`=Int32, `09`=Float64, `0b`=String, `0c`=Optional), revealing the oracle's own contracts: `wifi_channel_to_frequency(i32) -> Option<i32>` and `wifi_frequency_to_channel(Option<i32>) -> Option<i32>` (wider than the source's `u16`), and `ble_distance(i32, Option<i32>) -> f64` (vs the source's three `f64`s — a genuine ABI divergence, left for a follow-up). `cargo xtask oracle-differential` extracts the SHA-pinned oracle, prepares the Bionic sysroot, builds and runs the harness under qemu over a comprehensive sweep (channels −8..200; 2.4/5/6 GHz frequency edges and floor-division at 1 MHz resolution; out-of-`u16` and `None` inputs), and drift-checks the 580 executed-oracle rows against the committed ground truth `crates/bleradar-compat/tests/oracle/wifi_executed_vectors.tsv`. `tests/oracle_differential.rs` replays that baked ground truth against the reconstruction with no emulator (so it runs in ordinary `gates`): the source reproduces every executed-oracle output over its `u16` domain, and the oracle's wider domain collapses to `None` exactly where `u16` cannot represent the input — the one intentional divergence, asserted explicitly. Both contracts are now `DifferentiallyVerified` (compat registry + `docs/PARITY_COVERAGE.md`: differentially-verified 0 → 2, source-analogue 6 → 4). Falsified twice, each restored: mutating `wifi_channel_to_frequency(14)` to `2485` fails the CI test (`source Some(2485) != executed oracle Some(2484)`); tampering a committed vector fails both the CI test and the xtask drift gate, which re-executes the oracle and reports the first drifting row. The floating-point geodesy contracts (`haversine_m`, `bearing_deg`) were also executed and agree to within 1–2 ULP, but Bionic and host `libm` are not bit-identical for transcendentals, so a bit-exact baked assertion would be non-portable; they stay `SourceAnalog` pending an ULP-tolerant criterion. Two tiers keep this reproducible without weakening CI: the baked vectors + CI test run everywhere; the live regen/drift command is a `verify-android-live`-class tool (NDK + qemu + Bionic), documented with provenance in `docs/ORACLE_DIFFERENTIAL.md`. No production code or the committed APK changed — this adds executed-oracle evidence and an offline drift gate around the existing reconstruction.

73. **Extend the executed-oracle differential (decision #72) to the pure geodesy contracts `haversine_m` and `bearing_deg` with a physical tolerance.** `oracle_characterization.rs` had locked `haversine_m` against the oracle at a single coordinate pair and left `bearing_deg` with only "sampled outputs; exhaustive differential parity is pending". With the qemu executed-oracle mechanism already built, the marginal cost of broadening these was low, so a committed harness (`xtask/src/oracle_harness_geo.c`) calls the oracle's `haversine_m`/`bearing_deg` UniFFI scaffolding (both `(f64,f64,f64,f64) -> f64`, decoded from the `UNIFFI_META_*` blobs) over 308 coordinate pairs — eight edge cases (equator, one-degree steps, identical point, poles, antimeridian) plus 300 from a fixed-seed xorshift* sweep — emitting every input and output as raw f64 bits so the differential reconstructs each `f64` with no decimal round-trip. Unlike the WiFi contracts (#72, integer, bit-exact), these are transcendental: the oracle computes `sin`/`cos`/`asin`/`atan2` with Bionic `libm` and the reconstruction with the host `libm`, which are not bit-identical, so a bit-exact baked assertion would be non-portable (a broad bit-exact test flakes at the 1–2 ULP points, even though the existing single-point fixture happens to match). Measured over the 308 pairs: haversine agrees to a maximum of **11 nm** (1.1e-8 m; 275/308 bit-exact) and bearing to **8.5e-14°** (circular; 168/308 bit-exact, the larger ULP counts only at ill-conditioned near-antipodal pairs where `atan2` amplifies last-bit rounding — a few picodegrees, not a formula difference). `tests/oracle_geodesy_differential.rs` therefore asserts a *physical* tolerance — haversine `<1e-6 m`, bearing `<1e-8°` circular — which is robust across host `libm` versions yet far tighter than any real change (a wrong Earth radius or reworked formula moves metres/degrees). This is broad executed-oracle differential *coverage*, not a bit-exact promotion, so both contracts stay `SourceAnalog` (guarded by `geodesy_contracts_stay_source_analog_pending_bit_exact_parity`); the compat evidence is updated from "one sampled point"/"pending" to the executed-oracle bound. `cargo xtask oracle-differential` now builds, runs and drift-checks both the WiFi and geodesy harnesses (refactored to a shared `drift_check_harness` helper). Falsified twice, each restored: reverting the Earth radius to the pre-BF-002 `6_371_000.0` fails the CI test (`haversine_m(0,0,1,0)` differs by 0.154 m > 1e-6); tampering one committed vector fails the xtask drift gate (it re-executes the oracle and reports the first drifting row) while WiFi stays green. Two tiers as in #72: the baked vectors + CI test run in ordinary `gates` (no emulator); the live regen/drift command needs the NDK + qemu + Bionic. No production code or the committed APK changed.
