[package]
name = "bleradar-compat"
version = "0.5.0"
description = "Semantic parity-status registry for the BLE Radar native ABI surface."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
bleradar-core = { path = "../bleradar-core" }

[lints]
workspace = true
